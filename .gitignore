**/.DS_Store
.gstack/

# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Local tooling — not tracked (editor config, Claude Code skills/agents)
.vscode/
.claude/
/target-musl/

# Local env — never tracked (the root DATABASE_URL the Rust suite reads).
.env
.env.*
!.env.example
gateway/node_modules/
