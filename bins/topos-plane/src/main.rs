//! `topos-plane` — the OSS plane binary. A thin `axum` `main` that serves `router(state)` (no trust
//! decision, no raw object read in a handler). The server lands later.

fn main() {
    // Placeholder entrypoint — the axum router over plane-core lands later.
    eprintln!("topos-plane: not yet implemented (scaffold)");
    std::process::exit(0);
}
