{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "KeepAsYoursData",
  "description": "The `keep it as yours` describe — an `add <name>` that re-forks a RETAINED withdrawn/detached copy\ninto a NEW local skill with no upstream. Bare `add <name>` returns this preview; `--yes` re-adopts the\nbytes and returns an ordinary [`AddData`]. **INFERRED** (additive-only).",
  "type": "object",
  "properties": {
    "has_draft": {
      "description": "Whether a local draft rides along into the fork (a snapshotted or on-disk edit ahead of the base).",
      "type": "boolean"
    },
    "name": {
      "description": "The skill name to re-fork.",
      "type": "string"
    },
    "reason": {
      "description": "Why the local copy is retained (and no longer delivering here).",
      "$ref": "#/$defs/KeepReason"
    },
    "workspace": {
      "description": "That same workspace as a person addresses it. Absent alongside `workspace_id`, and\nwhenever no session on this installation names it. **INFERRED** (additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/WorkspaceRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "workspace_id": {
      "description": "The workspace the retained copy was followed in (its former upstream), if any.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "name",
    "reason",
    "has_draft"
  ],
  "$defs": {
    "KeepReason": {
      "description": "Why a `keep-as-yours` copy is retained but no longer live. **INFERRED** (additive value set).",
      "oneOf": [
        {
          "description": "Upstream withdrew the skill (archived, or its last delivering channel dropped it) — the agent dirs\nwere cleaned; the sidecar kept the bytes + any draft.",
          "type": "string",
          "const": "withdrawn-upstream"
        }
      ]
    },
    "WorkspaceRef": {
      "description": "THE WORKSPACE A VERB'S ACT REACHED — one field, one spelling, in every payload below.\n\n`<host>/<name>` is the address a person types and the one `topos workspace list` prints, so a\nconsumer can hand what it read here straight back to `--workspace`. The two halves ride apart\nbecause a slash inside a name would make parsing one string ambiguous, and the opaque\n`workspace_id` stays exactly what it was — the wire key — and is never spelled here.",
      "type": "object",
      "properties": {
        "host": {
          "description": "The server host the workspace lives on (`topos.sh`, `topos.example.com:3000`).",
          "type": "string"
        },
        "name": {
          "description": "The workspace's ADDRESS name — the slug, never the display name and never the opaque id.",
          "type": "string"
        }
      },
      "required": [
        "host",
        "name"
      ]
    }
  }
}
