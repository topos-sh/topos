{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PullData",
  "description": "`pull` result — per-skill currency state plus the reviewer-queue count. **PINNED.**",
  "type": "object",
  "properties": {
    "proposals_awaiting": {
      "description": "Open proposals on your followed skills (v0 is single-approver — any rostered member may review, so\nthe count is all open-non-stale proposals across what you follow, not a reviewer-assignment queue).",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "skills": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/PullSkill"
      }
    }
  },
  "required": [
    "skills",
    "proposals_awaiting"
  ],
  "$defs": {
    "Conflict": {
      "description": "The DIVERGED panel (local draft vs newer remote). **INFERRED fields.**",
      "type": "object",
      "properties": {
        "local_version_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[0-9a-f]{64}$"
        },
        "remote_version_id": {
          "description": "The remote version the draft diverged from.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      "required": [
        "remote_version_id"
      ]
    },
    "ConflictPathKind": {
      "description": "How a single path conflicts. **INFERRED value set.**",
      "oneOf": [
        {
          "description": "A textual three-way overlap — diff3 markers were written at the path.",
          "type": "string",
          "const": "content"
        },
        {
          "description": "A non-UTF-8 file with a true three-way divergence — theirs kept at the path, mine in a sidecar.",
          "type": "string",
          "const": "binary_content"
        },
        {
          "description": "Mine modified the file; theirs deleted it — mine kept at the path.",
          "type": "string",
          "const": "modify_delete"
        },
        {
          "description": "Mine deleted the file; theirs modified it — theirs kept at the path.",
          "type": "string",
          "const": "delete_modify"
        },
        {
          "description": "Both sides added the path with different content — theirs at the path, mine in a sidecar.",
          "type": "string",
          "const": "add_add"
        },
        {
          "description": "A consent-significant mode disagreement — theirs' bytes + mode at the path.",
          "type": "string",
          "const": "mode_mode"
        },
        {
          "description": "A side (or the merged output) exceeded the client size cap — theirs at the path, mine in a sidecar.",
          "type": "string",
          "const": "oversize"
        }
      ]
    },
    "ConflictPathReport": {
      "description": "One conflicting path in a [`MergeReport`]. **INFERRED** — `kind` reuses the persisted vocabulary.",
      "type": "object",
      "properties": {
        "kind": {
          "$ref": "#/$defs/ConflictPathKind"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "path",
        "kind"
      ]
    },
    "Generation": {
      "description": "The internal anti-replay counter `(epoch, seq)`. NEVER rendered to a user; the\nagent consumes `expected`/`current` on a CONFLICT.",
      "type": "object",
      "properties": {
        "epoch": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "seq": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "epoch",
        "seq"
      ]
    },
    "MergeReport": {
      "description": "The author-merge disclosure (the `merged` / `conflicted` outcomes of a diverged draft). **INFERRED\nfields** — the spec pins the merge semantics (deterministic, author-only, conflict-blocks-publish),\nnot this exact shape.",
      "type": "object",
      "properties": {
        "base_version_id": {
          "description": "The three-way base (the draft's fork point).",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "clean": {
          "description": "Whether the merge was clean (`true` → draft-on-current, publishable) or blocked (`false`).",
          "type": "boolean"
        },
        "conflicts": {
          "description": "The conflicting paths when `clean` is `false` — the agent's resolution checklist.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ConflictPathReport"
          }
        },
        "drop_diff": {
          "description": "For the escape / no-base 2-way fallback: a unified diff of what the chosen side drops vs the other.",
          "type": [
            "string",
            "null"
          ]
        },
        "result_digest": {
          "description": "The merged/conflict tree's `bundle_digest`.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "result_version_id": {
          "description": "The forward 1-parent commit carrying the merged (or conflict-marked) tree.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "theirs_version_id": {
          "description": "`current` (theirs) the draft was merged onto.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      "required": [
        "base_version_id",
        "theirs_version_id",
        "result_version_id",
        "result_digest",
        "clean"
      ]
    },
    "Offer": {
      "description": "The re-disclosed bytes a `pull` offers (confirm-each / first-receive). **INFERRED fields** — the\nspec pins that the offer re-discloses + re-binds the digest, not its exact shape.",
      "type": "object",
      "properties": {
        "bundle_digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "version_id": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      "required": [
        "version_id",
        "bundle_digest"
      ]
    },
    "PullAction": {
      "description": "What `pull` did / offers for a skill. **INFERRED value set** — the four-state machine pins the\nsemantics (CURRENT / BEHIND / DRAFT / DIVERGED) but not these exact tokens.",
      "oneOf": [
        {
          "description": "State ① — already current; nothing to do.",
          "type": "string",
          "const": "up_to_date"
        },
        {
          "description": "State ② clean — auto fast-forwarded to the new bytes.",
          "type": "string",
          "const": "fast_forwarded"
        },
        {
          "description": "State ② confirm-each / first-receive — a one-tap offer is waiting.",
          "type": "string",
          "const": "offered"
        },
        {
          "description": "State ④ — a local draft conflicts with a newer remote (surfaced, not yet resolved — e.g. a\nconfirm-each follower's bare sweep, which offers the merge rather than running it).",
          "type": "string",
          "const": "diverged"
        },
        {
          "description": "State ④ resolved cleanly — a three-way merge (or the escape) landed a draft-on-current.",
          "type": "string",
          "const": "merged"
        },
        {
          "description": "State ④ resolved with conflicts — a complete conflict tree was materialized and publish is blocked\nuntil the author resolves (or escapes).",
          "type": "string",
          "const": "conflicted"
        },
        {
          "description": "A transient local hold (e.g. a local go-back is pinned).",
          "type": "string",
          "const": "held"
        },
        {
          "description": "UPSTREAM withdrew the skill (archived, or its last delivering channel dropped it): the agent\ndirs were cleaned; the sidecar keeps the bytes + any draft delta (\"keep it as yours\" is a\nnarration away).",
          "type": "string",
          "const": "withdrawn"
        },
        {
          "description": "The PERSON detached the skill (an unfollow, or a channel leave that lapsed it) on some\ndevice: this copy froze in place — bytes untouched, delivery ended.",
          "type": "string",
          "const": "detached"
        },
        {
          "description": "THIS DEVICE excludes the skill (\"not on this device\"): the agent dirs are clear here, the\nperson keeps receiving it everywhere else, and following it here lifts the exclusion.",
          "type": "string",
          "const": "excluded"
        }
      ]
    },
    "PullSkill": {
      "description": "One followed skill's pull state. `observed`/`applied`/`action`/`offer`/`conflict` are PINNED by\nname; the *value enums* (`PullAction`) and the `offer`/`conflict` field shapes are INFERRED.",
      "type": "object",
      "properties": {
        "action": {
          "$ref": "#/$defs/PullAction"
        },
        "applied": {
          "description": "Highest generation actually materialized to disk.",
          "$ref": "#/$defs/Generation"
        },
        "conflict": {
          "anyOf": [
            {
              "$ref": "#/$defs/Conflict"
            },
            {
              "type": "null"
            }
          ]
        },
        "merge": {
          "description": "Present for the author-merge outcomes (`merged` / `conflicted`) — the resolution disclosure.",
          "anyOf": [
            {
              "$ref": "#/$defs/MergeReport"
            },
            {
              "type": "null"
            }
          ]
        },
        "observed": {
          "description": "The generation the plane most recently served — the sync target.",
          "$ref": "#/$defs/Generation"
        },
        "offer": {
          "anyOf": [
            {
              "$ref": "#/$defs/Offer"
            },
            {
              "type": "null"
            }
          ]
        },
        "skill": {
          "type": "string"
        },
        "workspace_id": {
          "description": "The workspace this followed skill lives in, or `None` for a targeted go-back / local-only pull that\nhas no follow entry. A pulled skill is normally followed (so `Some`), but `pull <skill>@<hash>` on an\nunfollowed copy has none — `Option` keeps that honest and stays symmetric with [`SkillEntry`]. Names\nthe workspace so a session-start sweep does not show two same-named skills indistinguishably.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "skill",
        "observed",
        "applied",
        "action"
      ]
    }
  }
}
