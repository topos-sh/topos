{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ListData",
  "description": "`list` result — the inventory, per scope. The scopes shown in full ride `scopes`; whatever the\ninvocation does not show rides ONE summary (`machine_summary` / `untracked_summary`) so nothing\nis invisible. The optional views (`untracked` / `remote` / `detail` / `agent_view`) fill only\nunder their flag. **The stable shape** (see the section note: it replaced the pre-1.0 bucket\npayload outright); additive-only from here.",
  "type": "object",
  "properties": {
    "agent_view": {
      "description": "The agent-eye view (`list -a <slug>`).",
      "anyOf": [
        {
          "$ref": "#/$defs/AgentView"
        },
        {
          "type": "null"
        }
      ]
    },
    "also_signed_in": {
      "description": "The OTHER workspaces this machine is signed into, present only under `--remote`. The\ncatalog view acts on one workspace; this names the rest so none of them is invisible, and\nthe TTY closes the view with the spelling that shows theirs.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/WorkspaceRef"
      }
    },
    "detail": {
      "description": "The one-skill deep dive (`list <name>`).",
      "anyOf": [
        {
          "$ref": "#/$defs/ListDetail"
        },
        {
          "type": "null"
        }
      ]
    },
    "footprint": {
      "description": "Only present under `--footprint`: topos-owned paths outside skill dirs.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "forge": {
      "description": "The last auto-update check of every external source the shown scopes name.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/ForgeSource"
      }
    },
    "machine_summary": {
      "description": "The machine scope's one-line summary, present only when the machine scope is NOT shown in\nfull (the default view inside a project).",
      "anyOf": [
        {
          "$ref": "#/$defs/ListScopeSummary"
        },
        {
          "type": "null"
        }
      ]
    },
    "remote": {
      "description": "Per-workspace catalog view (only under `--remote`). ONE workspace — the one the\ninvocation acts on.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/RemoteWorkspace"
      }
    },
    "scopes": {
      "description": "The scope sections shown in full, render order (here-scope first).",
      "type": "array",
      "items": {
        "$ref": "#/$defs/ListScope"
      }
    },
    "signed_in": {
      "description": "Signed-in state — the TTY renders the static remote pointer from it.",
      "type": "boolean"
    },
    "truncated": {
      "description": "The buckets that were row-capped (`--limit`/`--offset`, or the `--json` default page), one\nmarker per capped bucket. The page applies PER BUCKET; the `NEXT_PAGE` next action's argv\nfetches the next page. Empty (and omitted) on an uncapped list.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/BucketTruncation"
      }
    },
    "untracked": {
      "description": "Full untracked discoveries (only under `--untracked`).",
      "type": "array",
      "items": {
        "$ref": "#/$defs/UntrackedEntry"
      }
    },
    "untracked_summary": {
      "description": "The untracked discoveries' one-line summary (absent when nothing untracked was found, or\nwhen the untracked listing itself is shown).",
      "anyOf": [
        {
          "$ref": "#/$defs/UntrackedSummary"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "scopes",
    "signed_in"
  ],
  "$defs": {
    "AgentView": {
      "description": "The agent-eye view (`list -a <slug>`): for one harness from this folder, each skills dir it\nreads and what sits in it. Deliberately spans both scopes. **PINNED.**",
      "type": "object",
      "properties": {
        "agent": {
          "description": "The harness's registry slug.",
          "type": "string"
        },
        "agent_name": {
          "description": "The harness's human-readable name.",
          "type": "string"
        },
        "dirs": {
          "description": "The skills dirs the harness reads from this folder, each with its entries.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AgentViewDir"
          }
        }
      },
      "required": [
        "agent",
        "agent_name",
        "dirs"
      ]
    },
    "AgentViewDir": {
      "description": "One skills dir in an [`AgentView`]. **PINNED.**",
      "type": "object",
      "properties": {
        "entries": {
          "description": "The skill dirs sitting in it.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AgentViewEntry"
          }
        },
        "path": {
          "description": "The dir's path.",
          "type": "string"
        },
        "scope": {
          "description": "`\"user\"` (a home dir) or `\"project\"` (under this folder).",
          "type": "string"
        }
      },
      "required": [
        "path",
        "scope",
        "entries"
      ]
    },
    "AgentViewEntry": {
      "description": "One entry of an [`AgentViewDir`]. **PINNED.**",
      "type": "object",
      "properties": {
        "managed": {
          "description": "What manages it: `\"<file>:<row-key>\"` for a manifest row, `\"feed <host>/<ws>\"` for a\nworkspace feed, `\"built-in\"` for the CLI's own placed meta-skill; `None` = untracked.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The skill dir's name.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ]
    },
    "BucketTruncation": {
      "description": "One row-capped bucket in a paged [`ListData`]. **INFERRED** (additive).",
      "type": "object",
      "properties": {
        "bucket": {
          "description": "The capped bucket's name (a scope name — `project` / `machine` — or `untracked` /\n`remote` / `agent`). `remote` sums the drop across every workspace's skills, `agent`\nacross every dir's entries: one bucket per view, whatever it nests.",
          "type": "string"
        },
        "shown": {
          "description": "Rows emitted on this page.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "total": {
          "description": "Total rows in the bucket before paging.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "bucket",
        "shown",
        "total"
      ]
    },
    "ConflictReason": {
      "description": "Why a merge could not be applied cleanly. **INFERRED value set.**",
      "oneOf": [
        {
          "description": "A genuine three-way merge with at least one unresolved path.",
          "type": "string",
          "const": "three_way"
        },
        {
          "description": "Unrelated histories — no recorded base; a 2-way manual choice is required.",
          "type": "string",
          "const": "no_base"
        }
      ]
    },
    "DivergedCopy": {
      "description": "ONE copy of a bundle whose edits disagree with another copy's, in the two spellings every\nsurface that names such a copy uses: the folder as a person reads it, and the `--dest` value\nthat names it back on a command line. The same pair the placement freeze's refusal prints —\none vocabulary, so the deep dive and the refusal never describe one state two ways.",
      "type": "object",
      "properties": {
        "dest": {
          "description": "`.agents/skills` · `~/.claude/skills` — the `--dest` value naming this copy.",
          "type": "string"
        },
        "display": {
          "description": "`project/.agents/skills/coolify-deploy` · `~/.claude/skills/coolify-deploy`.",
          "type": "string"
        }
      },
      "required": [
        "display",
        "dest"
      ]
    },
    "ForgeSource": {
      "description": "One external source's last auto-update check — the answer to \"is this even working?\", computed\nentirely from local state. Every check is recorded whatever its outcome, so a source that has\ngone quiet is visible here long before the silent sweep is willing to interrupt a session over\nit. **INFERRED** (additive).",
      "type": "object",
      "properties": {
        "answered_at": {
          "description": "When it last ANSWERED (epoch millis). Absent = it never has.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "checked_at": {
          "description": "When it was last checked (epoch millis), whatever came of that check.",
          "type": "integer",
          "format": "int64"
        },
        "commit": {
          "description": "The commit the last answering check saw.",
          "type": [
            "string",
            "null"
          ]
        },
        "error": {
          "description": "Why the last check failed. Absent = it succeeded.",
          "type": [
            "string",
            "null"
          ]
        },
        "gone": {
          "description": "The failure is FINAL — the forge says this source is gone, so it is not retried until the\nrow that names it changes.",
          "type": "boolean"
        },
        "source": {
          "description": "The source a manifest row names (`<host>/<owner>/<repo>`).",
          "type": "string"
        }
      },
      "required": [
        "source",
        "checked_at"
      ]
    },
    "ListDetail": {
      "description": "The deep answer for ONE skill (`topos list <name>`): which file and line-key (or which feed)\ndelivers it, the version and any pin, where its bytes are, and its state. **PINNED.**",
      "type": "object",
      "properties": {
        "attribution": {
          "description": "The delivery attribution (`assigned by <name>` / `picked by you`), when known.",
          "type": [
            "string",
            "null"
          ]
        },
        "conflict_copy": {
          "description": "For a BLOCKED bundle: the workbench folder holding the stopped merge, spelled the way the\ndeep dive prints it. The receipt that stopped the merge named this folder once; this is\nwhere a reader gets it back after that receipt has scrolled away — or after a silent\nsession-start sweep raised the block with no receipt to read at all. Present only when the\nconflict record itself names a folder (never derived from the bundle's name, which two\nbundles can share). **INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "conflict_reason": {
          "description": "Why that merge stopped — the recorded reason, so the folder above is described the one way\nthe receipt already described it (`three_way`: both versions marked up in it; `no_base`:\nthis person's files with the team's beside them). Absent exactly when `conflict_copy` is.\n**INFERRED** (additive).",
          "anyOf": [
            {
              "$ref": "#/$defs/ConflictReason"
            },
            {
              "type": "null"
            }
          ]
        },
        "diverged": {
          "description": "The copies whose edits DISAGREE (always ≥ 2 when present) — the state the inventory row\nreports as `draft in N folders that disagree` and sends the reader here to resolve. Each\ncarries the folder as a person reads it and the `--dest` value that names it back, so the\nanswer can offer the per-copy acts. Absent whenever at most one copy is edited.\n**INFERRED** (additive).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/DivergedCopy"
          }
        },
        "drafted": {
          "description": "THIS checkout carries a draft — bytes ahead of the version it stands at, in any of its\nfolders. The one draft rule every surface reads (bytes against the lock), so a settled\ndraft nobody has touched since the last sweep still counts as unshared work.\n**INFERRED** (additive).",
          "type": "boolean"
        },
        "feed": {
          "description": "The feed origin, when no row names it (`<host>/<workspace>` — the workspace whose feed\ndelivers it).",
          "type": [
            "string",
            "null"
          ]
        },
        "folders": {
          "description": "For a NOT-managed answer: the folders of unmanaged copies discovered on disk (the known\nagents' skill dirs, matched by name) — `topos add <folder>` manages one. **INFERRED**\n(additive).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "harnesses": {
          "description": "For an `mcp` bundle: the per-agent config entries this machine records (the placed file +\nstate), shown instead of placement dirs. **INFERRED** (additive).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/McpAgentState"
          }
        },
        "kind": {
          "description": "The catalog bundle kind (`\"mcp\"` for a config-placed MCP-server bundle). Absent ⇒\n`\"skill\"`. **INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "managed": {
          "description": "`false` = NO row in any visible scope manages this name (and it is not the built-in): the\nanswer is the not-managed headline plus `folders`, and every managed-only field above is\nabsent/at its baseline. Omits when `true` (the pre-existing managed shape). **INFERRED**\n(additive).",
          "type": "boolean"
        },
        "mcp_unreachable": {
          "description": "For an `mcp` bundle whose row freezes a `dest` that names no config file topos can edit:\nwhy the bundle reaches NO agent. Absent whenever it reaches at least one — a row with\nsomewhere to land says nothing here. **INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The bundle's name.",
          "type": "string"
        },
        "pin": {
          "description": "A row's version pin, when one is spelled.",
          "type": [
            "string",
            "null"
          ]
        },
        "placements": {
          "description": "The placement directories this machine holds for it.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "scope": {
          "description": "The scope section that answered (`\"project\"` / `\"machine\"`) — the spelling every suggested\ncommand must honor: a machine-scope answer spells `topos update -g`. **INFERRED**\n(additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "source_file": {
          "description": "The manifest FILE whose row delivers it, when a row does (a path).",
          "type": [
            "string",
            "null"
          ]
        },
        "source_key": {
          "description": "The row's spelled KEY in that file (the joined reference).",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "description": "The line's state (the resolution's own vocabulary).",
          "$ref": "#/$defs/StatusItemState"
        },
        "twin": {
          "description": "The OTHER reachable scope's checkout of the SAME bundle, when it holds one — a separate\ncopy with its own draft state, which the answer names so a reader never mistakes one\ncheckout for both. Absent whenever only this scope tracks it. **INFERRED** (additive).",
          "anyOf": [
            {
              "$ref": "#/$defs/ScopeTwin"
            },
            {
              "type": "null"
            }
          ]
        },
        "version": {
          "description": "The applied version, when applied locally: a file bundle's commit (64-hex), or a connected\nserver's catalog revision (`mcpr_…`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "state"
      ]
    },
    "ListScope": {
      "description": "One scope section of a [`ListData`] — the rows one manifest delivers. **PINNED.**",
      "type": "object",
      "properties": {
        "manifest": {
          "description": "The governing manifest file, spelled as a person standing here reads it: a project manifest\nrelative to the working directory (`./topos.toml`, `../topos.toml`), the machine file\n`~`-abbreviated. `None` = no machine-wide file — nothing is demanded machine-wide (`topos\nlogin` writes a feed row on this machine's first connection to a workspace).",
          "type": [
            "string",
            "null"
          ]
        },
        "orphans": {
          "description": "Records this scope's store still holds — with skill folders or config entries still\nstanding — that NO row in this manifest, and no feed, demands any more.\n\nAn inventory built from manifest rows alone could not show these, and the sweep's orphan\nresolution deliberately passes over a record whose config entries still stand (they are\nplaced, not abandoned). Between the two, an MCP server could sit live in an agent's config\nwith no surface naming it and no command offered for it. One line each, never a section:\nthe thing standing, and the way out. **INFERRED** (additive).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/OrphanRecord"
          }
        },
        "rows": {
          "description": "The scope's inventory rows.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SkillEntry"
          }
        },
        "scope": {
          "description": "`\"project\"` (the nearest `topos.toml` covering the working directory) or `\"machine\"`.",
          "type": "string"
        }
      },
      "required": [
        "scope",
        "rows"
      ]
    },
    "ListScopeSummary": {
      "description": "The one-line summary of a scope the invocation does not show in full. **PINNED.**",
      "type": "object",
      "properties": {
        "command": {
          "description": "The exact command that expands the summary into the full section.",
          "type": "string"
        },
        "skills": {
          "description": "Skills the scope delivers.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "updates_pending": {
          "description": "How many of them sit behind their last-known served target.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "skills",
        "updates_pending",
        "command"
      ]
    },
    "McpAgentState": {
      "description": "One agent's (harness's) state for a config-placed (`mcp`) bundle on this installation — its\nconfig ENTRY, projected onto [the one outcome vocabulary](TargetOutcome). **INFERRED**\n(additive).",
      "type": "object",
      "properties": {
        "agent": {
          "description": "The harness registry slug (e.g. `claude-code`, `cursor`).",
          "type": "string"
        },
        "file": {
          "description": "The config file the entry lives in (the `list <name>` deep dive shows it; receipts omit\nit).",
          "type": [
            "string",
            "null"
          ]
        },
        "note": {
          "description": "A short human-readable qualifier (why not-supported / how a change goes live), when one\nexists.",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "description": "What this bundle's entry in that agent's config looks like after the run.",
          "$ref": "#/$defs/TargetOutcome"
        }
      },
      "required": [
        "agent",
        "state"
      ]
    },
    "OrphanRecord": {
      "description": "One record nothing demands whose bytes or entries still stand (see [`ListScope::orphans`]).\n**INFERRED** (additive).",
      "type": "object",
      "properties": {
        "kind": {
          "description": "The catalog bundle kind (`\"mcp\"` for a config-placed bundle). Absent ⇒ an ordinary skill.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The bundle's name — what a person calls it, never the store id.",
          "type": "string"
        },
        "standing": {
          "description": "Where it still stands: the config files its entries sit in, or the folders holding its\ncopies. Display paths, `~`-abbreviated.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "name"
      ]
    },
    "RemoteAdoption": {
      "description": "This machine's adoption state for a `--remote` catalog skill. **PINNED value set.**",
      "oneOf": [
        {
          "description": "A row in the here-scope delivers it.",
          "type": "string",
          "const": "adopted-here"
        },
        {
          "description": "Only the machine scope delivers it (seen from inside a project).",
          "type": "string",
          "const": "adopted-on-machine"
        },
        {
          "description": "No scope covering the working directory delivers it — `topos add` records the demand.",
          "type": "string",
          "const": "not-adopted"
        },
        {
          "description": "Adopted, but the catalog `current` is newer than what is applied here — `topos update`\nadvances it.",
          "type": "string",
          "const": "update-available"
        }
      ]
    },
    "RemoteChannel": {
      "description": "One channel in a [`RemoteWorkspace`]. **PINNED.**",
      "type": "object",
      "properties": {
        "adopted_in": {
          "description": "The manifest file whose row adopts this channel here, when one does.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The channel's name.",
          "type": "string"
        },
        "skills": {
          "description": "How many skills it references.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "name",
        "skills"
      ]
    },
    "RemoteSkill": {
      "description": "One catalog skill in a [`RemoteWorkspace`]. **PINNED.**",
      "type": "object",
      "properties": {
        "kind": {
          "description": "The catalog's bundle kind (`\"skill\"` · `\"mcp\"`) — an OPEN string, a CLOSED vocabulary at\nthe server that mints it. A listing renders it; the rows that DELIVER a bundle branch on\nit to choose the delivery mechanics.",
          "type": "string"
        },
        "name": {
          "description": "The skill's catalog name (the reference leaf).",
          "type": "string"
        },
        "open_proposals": {
          "description": "Open, non-stale proposal count on the skill. Always zero for a connected server: what it\nholds is published in the catalog, never proposed to this workspace.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "state": {
          "description": "This machine's adoption state for the skill.",
          "$ref": "#/$defs/RemoteAdoption"
        },
        "version_id": {
          "description": "What the workspace shares this bundle AT: a file bundle's `current` commit (64-char\nlowercase hex), or a connected server's catalog revision (`mcpr_` + 32 lowercase hex).",
          "type": "string",
          "pattern": "^([0-9a-f]{64}|mcpr_[0-9a-f]{32})$"
        }
      },
      "required": [
        "name",
        "kind",
        "version_id",
        "open_proposals",
        "state"
      ]
    },
    "RemoteWorkspace": {
      "description": "One workspace's catalog view under `list --remote` — its channels and skills, each annotated\nwith this machine's adoption state. Metadata only — the catalog grants no bytes. **PINNED.**",
      "type": "object",
      "properties": {
        "channels": {
          "description": "The workspace's channels.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/RemoteChannel"
          }
        },
        "skills": {
          "description": "The workspace's catalog skills.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/RemoteSkill"
          }
        },
        "workspace": {
          "description": "The workspace this section is about, as a person addresses it.",
          "$ref": "#/$defs/WorkspaceRef"
        },
        "workspace_id": {
          "description": "The workspace's opaque id.",
          "type": "string"
        }
      },
      "required": [
        "workspace",
        "workspace_id",
        "channels",
        "skills"
      ]
    },
    "ScopeTwin": {
      "description": "The OTHER scope's checkout of one bundle: which scope holds it, and whether it carries edits.\nNo folder — the answer sends a reader to that scope's own `list`, which names its folders.",
      "type": "object",
      "properties": {
        "drafted": {
          "description": "Whether that copy carries edits of its own.",
          "type": "boolean"
        },
        "machine": {
          "description": "`true` = the MACHINE's copy (`-g`), `false` = this project's.",
          "type": "boolean"
        }
      },
      "required": [
        "machine",
        "drafted"
      ]
    },
    "SkillEntry": {
      "description": "A skill row. `<skill>@<version_id>` identity + `draft` are PINNED; the other field names INFERRED.",
      "type": "object",
      "properties": {
        "bundle_digest": {
          "description": "The byte-exact consent hash, shown alongside as evidence. All-zero for a bundle that has no\nfiles to hash.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "draft": {
          "description": "Local edits ahead of the version this entry is on.",
          "type": "boolean"
        },
        "draft_dir": {
          "description": "The FOLDER holding this row's edited copy — the draft — as the row prints it: written\nagainst the folder its manifest governs (`project/.claude/skills/<name>`) for a project\nrow, `~`-abbreviated for a machine-wide one. Present only when exactly one copy is edited;\nabsent when several disagree (`draft_diverged` says so instead) and on every non-draft row.\n**INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "draft_diverged": {
          "description": "How many folders hold edits that DISAGREE — copies none of which explains another, so no\nsingle one is the draft. Always ≥ 2 when present, and present only on a draft row whose\ncopies compete; `draft_dir` is the one-copy case. **INFERRED** (additive).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "kind": {
          "description": "The catalog bundle kind (`\"mcp\"` for a config-placed MCP-server bundle). Absent ⇒\n`\"skill\"`. **Additive.**",
          "type": [
            "string",
            "null"
          ]
        },
        "pending_proposals": {
          "description": "Open proposals, each as `<skill>@<version_id>`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "skill": {
          "type": "string"
        },
        "source": {
          "description": "Where the bytes come from — the ORIGIN, never the manifest that asked for it: the workspace\naddress (`topos.sh/acme`) for anything a workspace delivers, the repository\n(`github.com/owner/repo`) for a forge row, the folder as the manifest spells it for a local\none, `built-in` for the CLI's own meta-skill. **INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "source_health": {
          "description": "The health of the source named in [`Self::source`], when that source has STOPPED answering.\nAbsent = nothing is wrong (or the origin is not one that gets probed). **INFERRED**\n(additive).",
          "anyOf": [
            {
              "$ref": "#/$defs/SourceHealth"
            },
            {
              "type": "null"
            }
          ]
        },
        "source_missing": {
          "description": "The row's SOURCE FOLDER is gone: a local-path row whose folder no longer exists. Nothing\ncan apply it, so the row's only honest next step is dropping the line — never the update\nevery other never-applied row offers. **INFERRED** (additive).",
          "type": "boolean"
        },
        "status": {
          "description": "The update status of the local copy: `current` / `behind` / `draft` / `off`. Absent when\nno status is honestly claimable (never applied, pending, no delivery yet). **INFERRED**\n(additive).",
          "anyOf": [
            {
              "$ref": "#/$defs/SkillStatus"
            },
            {
              "type": "null"
            }
          ]
        },
        "version_id": {
          "description": "What this row is ON: a file bundle's approvable `@` token (the commit SHA-256), or a\nconnected server's catalog revision (`mcpr_` + 32 lowercase hex). ONE field carries both,\nbecause what a bundle's version IS belongs to its kind — and only the first is a token any\nverb takes.",
          "type": "string",
          "pattern": "^([0-9a-f]{64}|mcpr_[0-9a-f]{32})$"
        },
        "workspace_id": {
          "description": "The workspace this skill is followed in (its pointer scope), or `None` for a purely local,\nnever-followed `add`'d skill. Provenance so two same-named skills from different workspaces are\ndistinguishable; `--json` carries it flat, the TTY groups by it.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "skill",
        "version_id",
        "bundle_digest",
        "draft"
      ]
    },
    "SkillStatus": {
      "description": "A tracked skill's update status in [`SkillEntry`]. **INFERRED** (additive value set).",
      "oneOf": [
        {
          "description": "On the followed `current` (or a local skill at its own head), no local edits.",
          "type": "string",
          "const": "current"
        },
        {
          "description": "The followed workspace serves a newer `current` than this copy holds — `update` to advance.",
          "type": "string",
          "const": "behind"
        },
        {
          "description": "Local edits ahead of the version this copy is on.",
          "type": "string",
          "const": "draft"
        },
        {
          "description": "A machine-local `\"off\"` row withholds this bundle here — a standing statement of the file,\nnever a delivery. **Additive.**",
          "type": "string",
          "const": "off"
        },
        {
          "description": "A merge is undecided here (see [`StatusItemState::Blocked`]): this copy is neither current\nnor an ordinary draft, and publishing it is refused until one of the two exits is taken.\n**Additive.**",
          "type": "string",
          "const": "blocked"
        }
      ]
    },
    "SourceHealth": {
      "description": "A row's origin has STOPPED ANSWERING — the one fact about a bundle that no other column can\ncarry. Its presence is the failure; the copy on disk keeps working, and would keep reading\n`current` forever, while nothing is keeping it current any more.\n\nWhy the last ANSWER and not the last check: the sweep retries on its own cadence and its clock\nadvances on failure too, so \"last checked\" is always recent and reads like a blip to ignore.\nThe question a stopped source raises is how stale this copy is, and only the last answer\nmeasures that. **INFERRED** (additive).",
      "type": "object",
      "properties": {
        "answered_at": {
          "description": "When the source last ANSWERED (epoch millis). Absent = it never has.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        }
      }
    },
    "StatusItemState": {
      "description": "A resolved line's state — the offline resolution's shared vocabulary ([`ListDetail::state`]\nand the internal per-scope rows). **INFERRED value set** (additive).",
      "oneOf": [
        {
          "description": "The bytes are applied and current against the last-known delivered target (see\n`applied_as_of` — the offline cache's stamp, not a live confirmation).",
          "type": "string",
          "const": "applied"
        },
        {
          "description": "A newer target is known than what is applied — the next `update` lands it.",
          "type": "string",
          "const": "behind"
        },
        {
          "description": "Local edits sit ahead of the applied version (a draft).",
          "type": "string",
          "const": "local-edits"
        },
        {
          "description": "A merge is undecided here: the team published a version that changes lines this person also\nchanged, so the folders still hold THEIR version, publishing is refused, and the two exits\n(`update <name> --keep-mine` / `--reset`) are the only ways forward. The row's\nversion/digest name what the folders hold, never the team's.",
          "type": "string",
          "const": "blocked"
        },
        {
          "description": "A machine-local `\"off\"` row in the global manifest withholds this bundle from the feed\nhere (the everywhere-path is the web decline).",
          "type": "string",
          "const": "off"
        },
        {
          "description": "Referenced here but NOT deliverable with your current access — phrased from LOCAL\nknowledge only (no live session for its workspace); never from server confirmation.",
          "type": "string",
          "const": "not-available"
        },
        {
          "description": "The session that would deliver it is pending an owner's approval.",
          "type": "string",
          "const": "pending-session"
        },
        {
          "description": "An adopted feed this installation has never had a delivery from: the next `update` performs\nthe first exchange. What that exchange brings — including nothing — is not knowable here,\nso the line promises an exchange, never an apply.",
          "type": "string",
          "const": "no-delivery-yet"
        },
        {
          "description": "Not applied here yet — `topos update` applies it (or the state is not determinable\noffline).",
          "type": "string",
          "const": "unknown"
        }
      ]
    },
    "TargetOutcome": {
      "description": "**THE ONE OUTCOME VOCABULARY** — what a single managed TARGET looks like after a run.\n\nA bundle's bytes reach a machine as one of two target shapes: a placement DIRECTORY the bundle\nowns, or an ENTRY it owns inside a config file. They are the same question asked twice (\"what\ndoes the thing topos owns look like now?\"), and they used to answer in two unrelated word sets\n— dirs through the row's action, entries through a free-form per-agent string that a reader\nhad to know by heart. Both now project onto THIS set, and both render from\n[`TargetOutcome::word`], so one outcome cannot be called two things.\n\nEvery variant is DERIVED, never chosen at a render site: from the drift vocabulary the\nownership record is scanned into (absent / clean / modified / foreign / unscannable) plus the\none bit the record cannot hold — whether this run WROTE the target. **INFERRED**, and the value\nset is CLOSED: every consumer matches it exhaustively and there is no unknown arm, so a new\noutcome is a deliberate change that ships with a release — never a value a running client is\nexpected to tolerate.",
      "oneOf": [
        {
          "description": "Nothing was there and this run wrote it — the target's FIRST materialization here.",
          "type": "string",
          "const": "created"
        },
        {
          "description": "The target stood at what the record says topos wrote, and this run rewrote it: a version\ncaught up, or a hand-deleted target put back. Never `created` — the distinction is the\nwhole reason a person can tell a new install from a repair.",
          "type": "string",
          "const": "refreshed"
        },
        {
          "description": "Found byte-for-byte as recorded; nothing was written.",
          "type": "string",
          "const": "current"
        },
        {
          "description": "Changed since topos wrote it — a local edit, left exactly as it is.",
          "type": "string",
          "const": "drifted"
        },
        {
          "description": "Content topos holds no record of writing, standing where this bundle's target belongs.\nNever overwritten.",
          "type": "string",
          "const": "conflicting"
        },
        {
          "description": "The target could not be read safely, so nothing was decided about it — fail closed.",
          "type": "string",
          "const": "unprovable"
        },
        {
          "description": "The target left this run.",
          "type": "string",
          "const": "removed"
        },
        {
          "description": "The surface exists but this bundle was deliberately not placed there (no config of that\nkind at this scope, a `dest` that excludes it).",
          "type": "string",
          "const": "not-supported"
        }
      ]
    },
    "UntrackedEntry": {
      "description": "A discovered-but-unmanaged skill — known only by where it lives, not by any topos version yet.\nA skill dir belongs to a FOLDER, and a folder is read by zero or more installed agents; there is\nno single owning harness, so the entry carries the folder and its readers rather than one slug.",
      "type": "object",
      "properties": {
        "folder": {
          "description": "The skills folder holding it — the parent of [`Self::path`].",
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "original": {
          "description": "The folder this entry's bytes really live in, when [`Self::path`] is a LINK SHELL onto\nanother folder (a symlinked dir, or a dir whose `SKILL.md` links out) — the folder an `add`\nof this entry takes. Absent when the entry IS its own origin, which is the ordinary case.\nAbsolute here; the terminal abbreviates it under the home.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "The skill dir itself.",
          "type": "string"
        },
        "readers": {
          "description": "The registry slugs of every INSTALLED agent that reads [`Self::folder`], sorted. These are the\n`<name>@<slug>` / `-a <slug>` tokens. Every entry of one folder carries the same list.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "scope": {
          "description": "Where the skill dir was found: `user` (a global agent home) or `project` (the current repo).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "path",
        "folder",
        "readers",
        "scope"
      ]
    },
    "UntrackedSummary": {
      "description": "The one-line summary of the untracked discoveries a bare `list` does not itemize. **PINNED.**",
      "type": "object",
      "properties": {
        "command": {
          "description": "The exact command that shows the full listing.",
          "type": "string"
        },
        "folders": {
          "description": "The distinct folders holding them.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "skills": {
          "description": "Untracked skills discovered in the known agents' skill folders.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "skills",
        "folders",
        "command"
      ]
    },
    "WorkspaceRef": {
      "description": "THE WORKSPACE A VERB'S ACT REACHED — one field, one spelling, in every payload below.\n\n`<host>/<name>` is the address a person types and the one `topos workspace list` prints, so a\nconsumer can hand what it read here straight back to `--workspace`. The two halves ride apart\nbecause a slash inside a name would make parsing one string ambiguous, and the opaque\n`workspace_id` stays exactly what it was — the wire key — and is never spelled here.",
      "type": "object",
      "properties": {
        "host": {
          "description": "The server host the workspace lives on (`topos.sh`, `topos.example.com:3000`).",
          "type": "string"
        },
        "name": {
          "description": "The workspace's ADDRESS name — the slug, never the display name and never the opaque id.",
          "type": "string"
        }
      },
      "required": [
        "host",
        "name"
      ]
    }
  }
}
