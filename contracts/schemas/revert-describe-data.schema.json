{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RevertDescribeData",
  "description": "`revert <skill> --to <good>` (bare, no `--yes`) — the two-phase DESCRIBE of the forward move: what\nmoves, the generation, and whether good's bytes already equal current's (a byte-level no-op). Nothing\nis written on the describe. **INFERRED.**",
  "type": "object",
  "properties": {
    "current_generation": {
      "description": "The live `current` generation the forward move would advance from.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "current_version_id": {
      "description": "The version `current` holds now (what the forward move restores away from).",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "is_noop": {
      "description": "Whether good's bytes ALREADY equal current's (compared by verified bundle digest, not commit id):\na repeated identical revert is a byte-level no-op that moves nothing.",
      "type": "boolean"
    },
    "reverted_to": {
      "description": "The good version named by `--to` — the bytes the forward move restores.",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "skill": {
      "type": "string"
    },
    "skill_id": {
      "type": "string"
    }
  },
  "required": [
    "skill",
    "skill_id",
    "current_version_id",
    "reverted_to",
    "current_generation",
    "is_noop"
  ]
}
