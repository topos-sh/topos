{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ReviewDescribeData",
  "description": "`review <target>` (bare, no verdict) — the target describe: who, what, base, staleness, and the\ndiff against current. **INFERRED.**",
  "type": "object",
  "properties": {
    "base_version_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "diff": {
      "description": "The unified diff of the proposal against current (`current..<proposal>`). Under a byte\nbudget (`--max-bytes`, or the `--json` default cap) it carries only the leading whole-file\nsections that fit; `diff_truncated` says so and the `FETCH_FULL_DIFF` next action's argv\nre-runs the same diff uncapped through `topos diff`.",
      "type": "string"
    },
    "diff_truncated": {
      "description": "`true` when `diff` was byte-capped. Omits when `false` (the prior shape is unchanged).\n**INFERRED** (additive).",
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "proposal": {
      "description": "The review target handle, `<skill>@<version_id>`.",
      "type": "string"
    },
    "proposer": {
      "type": "string"
    },
    "skill": {
      "type": "string"
    },
    "stale": {
      "type": "boolean"
    },
    "workspace": {
      "description": "The workspace the proposal lives in, as a person addresses it. Absent when no session on\nthis installation names it. **INFERRED** (additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/WorkspaceRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "yours": {
      "description": "Whether the CALLER authored this proposal — when `true` the describe offers `--withdraw` (a\nfour-eyes author cannot approve their own version) and the renderer says \"your proposal\".",
      "type": "boolean"
    }
  },
  "required": [
    "proposal",
    "skill",
    "proposer",
    "message",
    "base_version_id",
    "stale",
    "yours",
    "diff"
  ],
  "$defs": {
    "WorkspaceRef": {
      "description": "THE WORKSPACE A VERB'S ACT REACHED — one field, one spelling, in every payload below.\n\n`<host>/<name>` is the address a person types and the one `topos workspace list` prints, so a\nconsumer can hand what it read here straight back to `--workspace`. The two halves ride apart\nbecause a slash inside a name would make parsing one string ambiguous, and the opaque\n`workspace_id` stays exactly what it was — the wire key — and is never spelled here.",
      "type": "object",
      "properties": {
        "host": {
          "description": "The server host the workspace lives on (`topos.sh`, `topos.example.com:3000`).",
          "type": "string"
        },
        "name": {
          "description": "The workspace's ADDRESS name — the slug, never the display name and never the opaque id.",
          "type": "string"
        }
      },
      "required": [
        "host",
        "name"
      ]
    }
  }
}
