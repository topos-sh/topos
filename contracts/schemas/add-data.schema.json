{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AddData",
  "description": "`add` (local, offline — no plane op, `receipt: null`). **INFERRED.**",
  "type": "object",
  "properties": {
    "bundle_digest": {
      "description": "The kernel digest of the bytes this add landed. Present whenever bytes landed — including\nan adopted MCP folder, which IS the bundle — and absent for a row that points at somebody\nelse's bytes.",
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[0-9a-f]{64}$"
    },
    "claim": {
      "description": "The IDENTITY CLAIM this add recorded (`--as`): a folder that already held a copy of a\nbundle this scope manages is now one of its places. Nothing in the folder changed and no\nversion was minted. Absent for every other add. **INFERRED** (additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/ClaimReceipt"
        },
        {
          "type": "null"
        }
      ]
    },
    "currency": {
      "description": "The auto-update-trigger outcome, present when adopting into a recognized harness attempted a\nsession-start trigger install — the honest disclosure of the (only) write `add` makes outside\n`~/.topos/`. `None` for a plain directory.",
      "anyOf": [
        {
          "$ref": "#/$defs/TriggerReport"
        },
        {
          "type": "null"
        }
      ]
    },
    "dest": {
      "description": "The destinations the row was frozen to (`-a`/`--dest`): skills folders as the manifest\nspells them (`~/`-abbreviated in the machine file, project-relative in a project file),\nor a config-placed bundle's config files. Empty when the add named no destination.\n**INFERRED** (additive-only).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dest_change": {
      "description": "What this add did to the destinations a row ALREADY recorded. Absent when the row was born\nby this add — that receipt leads with what landed — and absent when the add named no\ndestination at all. **INFERRED** (additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/DestChange"
        },
        {
          "type": "null"
        }
      ]
    },
    "dest_resolved": {
      "description": "Where those destinations RESOLVED to on this machine, when that differs from how the\nmanifest spells them.\n\n`dest` is the row's own spelling and must stay it — a manifest line is portable, and\n`~/.codex/config.toml` is what belongs in the file. But an env override (`$CODEX_HOME`,\n`$XDG_CONFIG_HOME`, `$CLAUDE_CONFIG_DIR`) moves where that spelling actually lands, and a\nreceipt naming the default pointed at a file that does not exist on the machine it was\nprinted on. Empty ⇒ nothing moved and the row's own spelling IS the resolved one.\n**INFERRED** (additive-only).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "display": {
      "description": "The workspace-qualified display name the receipt leads with (`@<ws>/<name>` at the\nmachine's one connected host, else `<host>/<ws>/<name>`). Absent for a non-workspace\nsource. **INFERRED** (additive-only).",
      "type": [
        "string",
        "null"
      ]
    },
    "governed_copy": {
      "description": "A GOVERNED COPY of the same upstream source already living in a connected workspace — the\ndedup suggestion on a remote import (\"acme already has this as `@acme/deploy`\"). Disclosed,\nnever blocking: the import proceeded as asked; the reference is the governed alternative.\n**Additive.**",
      "anyOf": [
        {
          "$ref": "#/$defs/GovernedCopy"
        },
        {
          "type": "null"
        }
      ]
    },
    "harness": {
      "description": "The harness topos recognized the adopted directory as (e.g. Claude Code), or `None` for a plain\ndirectory tracked in place. Disclosed so the agent can see whether auto-update was armed.",
      "anyOf": [
        {
          "$ref": "#/$defs/HarnessId"
        },
        {
          "type": "null"
        }
      ]
    },
    "harness_slug": {
      "description": "The registry slug (e.g. `cursor`) of the agent that owns the added dir, when ONE does: the\nrecognized harness, or the sole installed reader of its folder (then `harness` may be `None`).\nUnset for a folder several agents read, or none. Provenance/disclosure only.",
      "type": [
        "string",
        "null"
      ]
    },
    "machine_copy": {
      "description": "The MACHINE folder this add's own copy landed in, disclosed when a checkout at or above\nthis folder ALREADY delivers the same bundle — a machine-wide add then gives you a second\ncheckout beside the project's, which is a surprise worth stating at the moment it is\ncreated. Absolute (the wire's form; the receipt abbreviates it under the home). Absent for\nevery add that creates no second copy. **INFERRED** (additive-only).",
      "type": [
        "string",
        "null"
      ]
    },
    "manifest": {
      "description": "The MANIFEST this add edited — the trust rail's first half: a `topos.toml` path. Absent when\nno manifest line was written (an internal adopt). **Additive.**",
      "type": [
        "string",
        "null"
      ]
    },
    "mcp": {
      "description": "The MCP SERVER this add landed (`add --kind mcp`, or a subscribe to a workspace `mcp`\nbundle): what the gated document says it is, the endpoint every agent will call, and the\nagents the converge reached — typed, so a JSON consumer never parses the prose note.\nAbsent for every other add, and for a workspace subscribe whose delivery has not landed\nthe document yet. **INFERRED** (additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/McpServerSummary"
        },
        {
          "type": "null"
        }
      ]
    },
    "name": {
      "type": "string"
    },
    "note": {
      "description": "A disclosure the receipt leads with when the edit was not the plain row write: a\nredundant row NOT written (the feed already delivers it), an `\"off\"` switch deleted\ninstead of a row added, or a standing web decline this machine's row now overrides.\n**Additive.**",
      "type": [
        "string",
        "null"
      ]
    },
    "origin": {
      "description": "Where the skill was imported FROM, when `add` fetched it from a remote source. `None` for a\nlocally-adopted skill (a path or a discovered name).",
      "anyOf": [
        {
          "$ref": "#/$defs/SkillOrigin"
        },
        {
          "type": "null"
        }
      ]
    },
    "published_match": {
      "description": "The connected workspace that publishes the same NAME an `add <name>` just adopted from a\nlocal directory — the team-managed spelling, disclosed beside the local copy that landed.\nNever blocking (the adopt happened as asked); absent when no workspace, or more than one,\ncarries the name. **Additive.**",
      "anyOf": [
        {
          "$ref": "#/$defs/PublishedMatch"
        },
        {
          "type": "null"
        }
      ]
    },
    "reference": {
      "description": "The reference the manifest line stores (canonical where resolvable). **Additive.**",
      "type": [
        "string",
        "null"
      ]
    },
    "scope": {
      "description": "WHICH file it was, as a receipt names it: this folder's `./topos.toml` or the machine-wide\n`~/.topos/topos.toml`. Written together with `manifest` and absent wherever that is — the\npath is the machine-readable fact, this is the one a person reads. **INFERRED**\n(additive-only).",
      "anyOf": [
        {
          "$ref": "#/$defs/ReceiptScope"
        },
        {
          "type": "null"
        }
      ]
    },
    "skill_id": {
      "description": "The local sidecar record this add minted, when it minted one. ABSENT for a row that has no\nbytes of its own here — a feed, a channel, a whole repo: a row-only add mints no version\nhistory, and a zeroed id would claim one.",
      "type": [
        "string",
        "null"
      ]
    },
    "source": {
      "description": "WHERE THIS BUNDLE COMES FROM, in the spelling that identifies it forever: the full\nreference where a workspace or a forge governs it (`topos.sh/acme/code-review`,\n`github.com/owner/repo`), and the canonical folder on this machine where the bytes are the\nperson's own. One derivation for every arm — the receipt's `source:` line is this value\nverbatim. Absent only for an internal adopt that recorded no row. **INFERRED**\n(additive-only).",
      "type": [
        "string",
        "null"
      ]
    },
    "tracked": {
      "type": "boolean"
    },
    "triggers": {
      "description": "The breadth arming sweep's outcomes — one row per OTHER detected agent whose auto-update\ntrigger was (un)installed alongside the active adapter's (`currency` above). **Additive.**",
      "type": "array",
      "items": {
        "$ref": "#/$defs/TriggerReport"
      }
    },
    "unchanged": {
      "description": "This add changed NOTHING — the file already spelled exactly this row, or the folder was\nalready one of the bundle's places. The receipt then leads with that fact instead of an\ninstalled/added headline above a note retracting it: an act that did not happen may not\nhead the answer. `note` carries the sentence. **INFERRED** (additive-only).",
      "type": "boolean"
    },
    "undo": {
      "description": "The paste-ready inverse (`topos remove <ref>`). Empty when nothing is undoable. **Additive.**",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "version_id": {
      "description": "The base commit the local sidecar starts from. Absent wherever `skill_id` is.",
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[0-9a-f]{64}$"
    }
  },
  "required": [
    "name",
    "tracked"
  ],
  "$defs": {
    "ClaimReceipt": {
      "description": "`add <path> --as <bundle>` — the folder is recorded as one of the bundle's places, byte for\nbyte as it stands.",
      "type": "object",
      "properties": {
        "folder": {
          "description": "The claimed folder, as a printed line spells it (`~/`-abbreviated under the machine home).",
          "type": "string"
        },
        "state": {
          "description": "What the folder's bytes turned out to be, against the bundle's own history.",
          "$ref": "#/$defs/ClaimState"
        },
        "twin": {
          "description": "A DUPLICATE of the same bundle beside the claimed folder — the copy the engine had placed\nunder its collision-suffixed name. Absent when there was none.",
          "anyOf": [
            {
              "$ref": "#/$defs/ClaimTwin"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "folder",
        "state"
      ]
    },
    "ClaimState": {
      "description": "What a claimed folder holds, against the bundle's version history. **INFERRED value set.**",
      "oneOf": [
        {
          "description": "Exactly the version the bundle is on — the folder is already current.",
          "type": "string",
          "const": "current"
        },
        {
          "description": "Exactly an OLDER version in the bundle's history — the next update brings it current.",
          "type": "string",
          "const": "older"
        },
        {
          "description": "Bytes no version explains: they become this bundle's draft (snapshotted first).",
          "type": "string",
          "const": "edited"
        },
        {
          "description": "Edited, AND another folder holds different edits — the placement freeze. Nothing syncs\nuntil one copy is chosen.",
          "type": "string",
          "const": "frozen"
        }
      ]
    },
    "ClaimTwin": {
      "description": "The duplicate copy beside a claimed folder: the engine's own placement under its\ncollision-suffixed name.",
      "type": "object",
      "properties": {
        "folder": {
          "description": "The duplicate's folder, as a printed line spells it.",
          "type": "string"
        },
        "removed": {
          "description": "`true` when it was retired (it held no edits); `false` when it was kept because it did.",
          "type": "boolean"
        }
      },
      "required": [
        "folder",
        "removed"
      ]
    },
    "CurrencyKind": {
      "description": "What fires the update check for a harness — drives HONEST receipt copy (\"current by next session\",\n\"within about a minute\", …). `ExplicitPullOnly` is the honest-degrade floor.",
      "oneOf": [
        {
          "description": "A session-boundary hook runs the sweep (Claude Code's SessionStart; Hermes's\n`on_session_start`/`on_session_reset`) — updates are current when a session begins.",
          "type": "string",
          "const": "session_start"
        },
        {
          "description": "A harness-scheduled recurring job runs the sweep (OpenClaw's silent cron) — updates land\nwithin the cadence while the harness's scheduler is running.",
          "type": "string",
          "const": "scheduled"
        },
        {
          "description": "No automatic trigger is provably live — updates land on an explicit `topos update`.",
          "type": "string",
          "const": "explicit_pull_only"
        }
      ]
    },
    "DestChange": {
      "description": "The destination set of a STANDING row, changed by an add. Destinations EXTEND: what the row\nalready recorded stays, and what this add named joins it.",
      "type": "object",
      "properties": {
        "added": {
          "description": "The destinations this add ADDED, in the order the row now spells them.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "frozen": {
          "description": "The WHOLE set the row names now — stated only where this add was the first to freeze a row\nthat had been reaching every agent, so the reader sees exactly what replaced \"everywhere\".\nEmpty when the row already named destinations.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "added"
      ]
    },
    "GovernedCopy": {
      "description": "The dedup suggestion an [`AddData`] carries when a remote import's source is already governed\nin a connected workspace. **INFERRED** (additive-only).",
      "type": "object",
      "properties": {
        "name": {
          "description": "The skill's catalog name there.",
          "type": "string"
        },
        "reference": {
          "description": "The paste-ready reference, in the canonical host-qualified form\n(`<host>/<workspace>/<name>` — unambiguous however many servers this installation is\nlogged into).",
          "type": "string"
        },
        "same_path": {
          "description": "Whether the governed copy was imported from the exact same subdirectory (`false` = same\nrepository, different path).",
          "type": "boolean"
        },
        "workspace": {
          "description": "The workspace's address name.",
          "type": "string"
        }
      },
      "required": [
        "workspace",
        "name",
        "reference",
        "same_path"
      ]
    },
    "HarnessId": {
      "type": "string",
      "enum": [
        "claude-code",
        "openclaw",
        "hermes"
      ]
    },
    "McpServerSummary": {
      "description": "What an MCP `server.json` says, after the gate accepted it — the facts an applied `add`\nreceipt carries beside the undo, whether the document was adopted from a folder here or\ndelivered by a workspace subscribe. DERIVED from the document; the document itself is never\nechoed whole.\n**INFERRED** (additive-only).",
      "type": "object",
      "properties": {
        "agents": {
          "description": "The agents set up here that the row reaches, after any `dest` narrowing — the honest\nbreadth line, so nobody has to reverse-engineer which config files changed.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "auth": {
          "description": "The publisher's `_meta` auth word (`oauth` / `none`) when the document declared one.\nAbsent means it said nothing — which is NOT a claim that no credential is needed.",
          "type": [
            "string",
            "null"
          ]
        },
        "bundle": {
          "description": "The folder holding the document (the bundle IS its directory). Absent for a\nworkspace-subscribed server, whose bytes live in the scope's store rather than in a\nfolder of their own here.",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "The publisher's one-line summary (the registry caps it at 100 characters).",
          "type": "string"
        },
        "headers": {
          "description": "The names of the LITERAL headers every call will carry. A credential-shaped header never\nreaches here — the gate refuses the whole document instead.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "server": {
          "description": "The registry name (`io.github.<owner>/<server>`) — the server's own identity.",
          "type": "string"
        },
        "transport": {
          "description": "The transport, always `streamable-http` today: the one a shared bundle can promise, since\nthe same URL has to work from every machine.",
          "type": "string"
        },
        "url": {
          "description": "The endpoint an agent will call — always `https`, never a template.",
          "type": "string"
        },
        "version": {
          "description": "The version the document declares.",
          "type": "string"
        }
      },
      "required": [
        "server",
        "description",
        "version",
        "url",
        "transport"
      ]
    },
    "PublishedMatch": {
      "description": "The workspace spelling an [`AddData`] carries when the bare NAME a local adopt resolved is also\npublished in exactly one connected workspace. Distinct from [`GovernedCopy`], which matches an\nimport's UPSTREAM source; this one matches nothing but the name the user typed.\n**INFERRED** (additive-only).",
      "type": "object",
      "properties": {
        "identical": {
          "description": "Whether the adopted bytes are IDENTICAL to the version that workspace currently serves —\n`false` also covers \"not knowable here\" (an offline cache match carries no digest).",
          "type": "boolean"
        },
        "name": {
          "description": "The bundle's catalog name there (the name that matched).",
          "type": "string"
        },
        "reference": {
          "description": "The paste-ready reference, in the canonical host-qualified form\n(`<host>/<workspace>/<name>` — unambiguous however many servers this installation is\nlogged into).",
          "type": "string"
        },
        "workspace": {
          "description": "The workspace's address name.",
          "type": "string"
        }
      },
      "required": [
        "workspace",
        "name",
        "reference",
        "identical"
      ]
    },
    "ReceiptScope": {
      "description": "Which of the two unblended manifests an add recorded into. Same vocabulary as\n[`ListScope::scope`]. **INFERRED value set.**",
      "oneOf": [
        {
          "description": "The nearest `topos.toml` covering the working directory.",
          "type": "string",
          "const": "project"
        },
        {
          "description": "This machine's own `~/.topos/topos.toml` (`-g`).",
          "type": "string",
          "const": "machine"
        }
      ]
    },
    "SkillOrigin": {
      "description": "Where an adopted skill was imported FROM, when `add` fetched it from a remote source (a GitHub repo).\nAll fields are public provenance — never a secret — and travel with the adopted skill so the agent (and\na later re-sync) can see the upstream it came from. `None` on `AddData` for a locally-adopted skill.\n**INFERRED** (additive-only).",
      "type": "object",
      "properties": {
        "commit": {
          "description": "The resolved commit the bytes came from (best-effort — parsed from the fetched archive), if known.",
          "type": [
            "string",
            "null"
          ]
        },
        "git_ref": {
          "description": "The branch / tag / commit requested (`#<ref>` or a `/tree/<ref>/…` URL), if any.",
          "type": [
            "string",
            "null"
          ]
        },
        "license": {
          "description": "A LICENSE file found at the skill root or repo root, recorded as provenance (never injected into the\nbundle — the adopted bytes stay byte-exact to the repo). `None` if the source carried no license.",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "description": "The `<host>/<owner>/<repo>` the skill was imported from (e.g. `github.com/vercel-labs/agent-skills`).",
          "type": "string"
        },
        "subdir": {
          "description": "The skill's path within the repo (a monorepo subdir), if it was not the repo root.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "source"
      ]
    },
    "TriggerReport": {
      "description": "The result of (un)installing ONE agent's auto-update trigger — what was touched + the marker, so\nre-install is idempotent (the marker is a managed sentinel block in the config edit, NEVER in skill\nbytes). The one shape every trigger reports in, whichever machinery served it.",
      "type": "object",
      "properties": {
        "agent": {
          "description": "The agent's registry slug (`claude-code`, `cursor`, `hermes-agent`).",
          "type": "string"
        },
        "currency_kind": {
          "description": "Only an `active` state carries a live kind; every other state advertises the explicit-pull floor.",
          "$ref": "#/$defs/CurrencyKind"
        },
        "marker_id": {
          "description": "`topos` + agent slug + schema version + command identity.",
          "type": "string"
        },
        "note": {
          "description": "The consent step still owed, or the evidence-level caveat — `None` when nothing needs saying.",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "$ref": "#/$defs/TriggerState"
        },
        "touched_path": {
          "description": "The config file edited (never a skill dir).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "agent",
        "currency_kind",
        "marker_id",
        "state"
      ]
    },
    "TriggerState": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "active",
            "inactive",
            "degraded"
          ]
        },
        {
          "description": "An equivalent command exists without our marker — adopt-or-leave, never blind-append.",
          "type": "string",
          "const": "already_present_unmanaged"
        }
      ]
    }
  }
}
