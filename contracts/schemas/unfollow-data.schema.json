{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UnfollowData",
  "description": "`unfollow` (local — stop following `current`, keep the bytes as a frozen copy). **INFERRED.**",
  "type": "object",
  "properties": {
    "bytes_kept": {
      "description": "The local bytes are retained, not deleted.",
      "type": "boolean"
    },
    "following": {
      "type": "boolean"
    },
    "skill_id": {
      "type": "string"
    }
  },
  "required": [
    "skill_id",
    "following",
    "bytes_kept"
  ]
}
