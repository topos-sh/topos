{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RemoveData",
  "description": "`remove` — take skills off THIS device. A followed skill becomes a per-device exclusion (other\ndevices keep receiving it); an untracked local copy (or a never-published tracked one) is deleted\npermanently. **INFERRED.**",
  "type": "object",
  "properties": {
    "applied": {
      "description": "`true` on an apply (immediate for a followed clean skill, or `--yes`), `false` on the\ndescribe (nothing changed yet).",
      "type": "boolean"
    },
    "items": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RemoveItem"
      }
    },
    "undo": {
      "description": "APPLY receipts: the literal inverse command (paste-ready argv) — `topos follow <skill>`\nre-attaches the followed skills this removal excluded. Empty when nothing is undoable (a\npermanent delete) or on a describe. **INFERRED** (additive).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "uninstalled": {
      "description": "APPLY receipts of a feed-line drop: the bundles whose placed copies left this machine IN\nTHIS INVOCATION (the row edit uninstalls eagerly), one entry per bundle — with any edited\ncopy kept in place. Empty everywhere else. **INFERRED** (additive).",
      "type": "array",
      "items": {
        "$ref": "#/$defs/UninstalledBundle"
      }
    }
  },
  "required": [
    "items",
    "applied"
  ],
  "$defs": {
    "RemoveItem": {
      "description": "One skill in a [`RemoveData`]. **INFERRED.**",
      "type": "object",
      "properties": {
        "agent_dirs": {
          "description": "The agent directories cleaned (or, on the describe, that would be cleaned).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bytes_kept": {
          "description": "Whether the sidecar bytes are kept (a manifest edit keeps the tracked bytes as a frozen\ncopy; an untracked-local delete removes the only copy there is).",
          "type": "boolean"
        },
        "kind": {
          "description": "How the removal behaves for this skill.",
          "$ref": "#/$defs/RemoveKind"
        },
        "manifest": {
          "description": "The MANIFEST the removal edited (a `topos.toml` path). Absent when no manifest line was\ntouched. **Additive.**",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "note": {
          "description": "A removal-specific disclosure (the built-in skill's durable opt-out + its way back). Absent\nfor ordinary removals. **INFERRED** (additive).",
          "type": [
            "string",
            "null"
          ]
        },
        "workspace_id": {
          "description": "The workspace the exclusion is recorded in (a followed skill); absent for a local copy.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "kind",
        "bytes_kept"
      ]
    },
    "RemoveKind": {
      "description": "How `remove` treats one skill. **INFERRED value set.**",
      "oneOf": [
        {
          "description": "The manifest's own include line was deleted — delivery to this scope just ends. **Additive.**",
          "type": "string",
          "const": "manifest-removed"
        },
        {
          "description": "The machine-wide FEED line was deleted: the workspace's feed stops delivering here, and the\ncopies it delivered were uninstalled in the same invocation ([`RemoveData::uninstalled`] —\nedited copies kept in place). **Additive.**",
          "type": "string",
          "const": "feed-removed"
        },
        {
          "description": "A broader layer still provides the item, so an EXCLUDE line was recorded in the nearest\nmanifest (the one negative state). **Additive.**",
          "type": "string",
          "const": "manifest-excluded"
        },
        {
          "description": "A NARROWED removal (`-a`/`--dest`): the named destination(s) were subtracted from the\nrow's `dest` and that copy uninstalled; the row survives with the rest. **Additive.**",
          "type": "string",
          "const": "manifest-narrowed"
        },
        {
          "description": "An untracked local copy in an agent dir → permanent delete (no other copy exists).",
          "type": "string",
          "const": "untracked-local"
        },
        {
          "description": "A tracked, never-published local skill → permanent delete (the sidecar entry drops too).",
          "type": "string",
          "const": "tracked-local-permanent"
        }
      ]
    },
    "UninstalledBundle": {
      "description": "One bundle an APPLIED removal uninstalled (or would have, had its copies not been edited).\n**INFERRED** (additive).",
      "type": "object",
      "properties": {
        "destinations": {
          "description": "The destinations the copies left — skill folders, or a config-placed bundle's config\nfiles. Display paths, `~`-abbreviated. Empty when every copy was edited and kept.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "kept": {
          "description": "Locally-edited copies kept in place instead of uninstalled — display paths,\n`~`-abbreviated.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "kind": {
          "description": "The catalog bundle kind (`\"mcp\"` for a config-placed bundle). Absent ⇒ an ordinary skill.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The workspace-qualified display name (`@<ws>/<name>` at the machine's one connected host,\nelse `<host>/<ws>/<name>`); a non-workspace bundle's plain name.",
          "type": "string"
        },
        "remaining": {
          "description": "A NARROWED removal (`-a`/`--dest`): how many destinations the row still names after the\nsubtraction — the receipt's `— N folders remain` clause (config files for an MCP\nbundle). Absent on a whole-row removal. **INFERRED** (additive).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "name"
      ]
    }
  }
}
