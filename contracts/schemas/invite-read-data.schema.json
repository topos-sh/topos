{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "InviteReadData",
  "description": "`invite` (bare, no emails) — the no-mutation read of the workspace address + invite policy.\n**INFERRED.**",
  "type": "object",
  "properties": {
    "address": {
      "description": "The workspace address teammates paste to join.",
      "type": "string"
    },
    "changed": {
      "description": "Always `false` — a bare read sends nothing and changes nothing.",
      "type": "boolean"
    },
    "invite_policy": {
      "description": "The workspace's invite policy (`members` / `owners`) — who may invite.",
      "type": "string"
    }
  },
  "required": [
    "address",
    "invite_policy",
    "changed"
  ]
}
