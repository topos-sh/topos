{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "VerifyData",
  "description": "`verify <name>` — the LIVE check of one MCP server bundle, printed and never stored. The verdict\nis about this moment and this machine: it is not a delivery state, and nothing in the sidecar\nremembers it. **INFERRED** (additive-only).",
  "type": "object",
  "properties": {
    "address": {
      "description": "The endpoint dialed, when the check dialed one.",
      "type": [
        "string",
        "null"
      ]
    },
    "command": {
      "description": "The program run, as the command and its arguments, when the check ran one. No environment\nvalue ever appears here.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "detail": {
      "description": "The plain-words reason behind anything other than `responding` — the same words the printed\nline carries after its colon.",
      "type": [
        "string",
        "null"
      ]
    },
    "exit_code": {
      "description": "The process exit code this verdict produces: `0` responding · `3` sign-in required ·\n`4` not reachable · `5` not answering as an MCP server.",
      "type": "integer",
      "format": "uint8",
      "maximum": 255,
      "minimum": 0
    },
    "line": {
      "description": "The one sentence the terminal prints — relay it verbatim.",
      "type": "string"
    },
    "name": {
      "description": "The bundle's name.",
      "type": "string"
    },
    "protocol_version": {
      "description": "The MCP protocol revision the server answered on (`2026-07-28` for the current revision,\n`2025-11-25` for the handshake era). Absent when nothing answered.",
      "type": [
        "string",
        "null"
      ]
    },
    "sign_in": {
      "description": "WHICH of the two sign-in worlds the server lives in, on `sign_in_required` alone. Absent\nwherever the check could not read the server's own OAuth discovery documents end to end —\na chain nobody could read is never reported as the worse of the two, and it is absent on\nevery other state because they demand no sign-in at all.",
      "anyOf": [
        {
          "$ref": "#/$defs/SignInPath"
        },
        {
          "type": "null"
        }
      ]
    },
    "state": {
      "description": "The verdict.",
      "$ref": "#/$defs/VerifyState"
    },
    "target": {
      "description": "What was checked: `address` (an endpoint this machine dialed) or `program` (a package this\nmachine ran and spoke to over its pipes).",
      "type": "string"
    },
    "tools": {
      "description": "How many tools the server listed. Present only on `responding`.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0
    }
  },
  "required": [
    "name",
    "target",
    "state",
    "exit_code",
    "line"
  ],
  "$defs": {
    "SignInPath": {
      "description": "Who can complete the sign-in a server demands — the distinction the same `401` hides.\n**INFERRED value set** (additive-only).",
      "oneOf": [
        {
          "description": "Its authorization server registers clients on demand, so the agent app signs in on first\nuse with nothing arranged beforehand.",
          "type": "string",
          "const": "self_service"
        },
        {
          "description": "It accepts only clients or tokens registered in advance — a personal token, or an app an\nadministrator created. A person does that once, per machine or per organization; an agent\ncannot do it for them.",
          "type": "string",
          "const": "manual"
        }
      ]
    },
    "VerifyState": {
      "description": "The four things a live check can conclude. **INFERRED value set** (additive-only).",
      "oneOf": [
        {
          "description": "It answered as an MCP server and listed its tools.",
          "type": "string",
          "const": "responding"
        },
        {
          "description": "It refused without a sign-in — a HEALTHY server; topos holds no credential and never will.\nWHO can complete that sign-in is a second question, and [`VerifyData::sign_in`] is where it\nis answered.",
          "type": "string",
          "const": "sign_in_required"
        },
        {
          "description": "Nothing answered: the name did not resolve, the connection failed or timed out, or the\nserver is having trouble right now. Never a statement about the protocol.",
          "type": "string",
          "const": "not_reachable"
        },
        {
          "description": "Something answered, but not as an MCP server.",
          "type": "string",
          "const": "not_an_mcp_server"
        }
      ]
    }
  }
}
