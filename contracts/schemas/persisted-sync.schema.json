{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SyncState",
  "description": "`skills/<id>/sync.json` — the durable client sync state (the four-state sync machine's memory).\n**Fully pinned.** The four states (CURRENT / BEHIND / DRAFT / DIVERGED) are *derived* from\n`observed`/`applied` vs the working tree, never stored.",
  "type": "object",
  "properties": {
    "applied": {
      "description": "Highest generation actually MATERIALIZED — advances only after a successful swap. After a server\nrestore the served target may sit below this; that is a legitimate state the engine simply\napplies toward.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "base_commit": {
      "description": "The commit the working tree derives from (= the applied commit when clean).",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "draft_observed": {
      "description": "The draft work-tree digest the previous run OBSERVED for this bundle+scope (sha256 hex of\nthe one edited copy's bytes), or absent when no draft was standing. The settled-draft\nfan-out compares the current draft against it: unchanged across two runs means SETTLED, and\nonly a settled draft is copied onto the bundle's other placements in its scope — a mid-edit\nfile never spreads. **Additive optional** (absent in older documents; cleared when the\ndraft resolves).",
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[0-9a-f]{64}$"
    },
    "held": {
      "description": "A transient local pin (a `pull <skill>@<hash>` go-back) suppressing one auto fast-forward.",
      "type": "boolean"
    },
    "observed": {
      "description": "The generation the plane most recently served — the sync target the engine drives `applied`\ntoward.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "observed_version_id": {
      "description": "The `version_id` (the 64-hex commit) the served `observed` generation named — the target bytes\nthe engine materializes and re-verifies by digest on apply.",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "schema_version": {
      "type": "integer",
      "format": "uint32",
      "const": 1,
      "minimum": 0
    },
    "superseded": {
      "description": "The team `current` this machine's draft deliberately does not contain — written when a\n`--keep-mine` resolution commits bytes that do NOT already carry the served version, and\nCOMPUTED at that moment (the merge is re-run in memory over the chosen tree), never\nasserted from the fact that the command was typed. It is what makes a later `publish`\ndisclose the replacement instead of shipping it silently.\n\nMeaningful only while a draft stands: it is carried through a clean merge onto a newer\nteam version (the draft still drops the older one) and cleared by a reset, a landed\npublish, a go-back, a fast-forward, and by the sweep once the work tree equals the base.\n**Additive optional** (absent in older documents, and absent whenever nothing is dropped).",
      "anyOf": [
        {
          "$ref": "#/$defs/Superseded"
        },
        {
          "type": "null"
        }
      ]
    },
    "work_hash": {
      "description": "sha256 (lowercase hex) of the current harness-dir bytes (recomputed; cheap).",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  },
  "required": [
    "schema_version",
    "observed",
    "observed_version_id",
    "applied",
    "base_commit",
    "work_hash",
    "held"
  ],
  "$defs": {
    "Superseded": {
      "description": "The version a draft deliberately does not contain (see [`SyncState::superseded`]). **Field-set\npinned** (additive).",
      "type": "object",
      "properties": {
        "bundle_digest": {
          "description": "That version's `bundle_digest` — the render pin, so the publish describe can diff against\nit offline without re-deriving anything.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "version_id": {
          "description": "The `version_id` of the team `current` the draft drops.",
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      "required": [
        "version_id",
        "bundle_digest"
      ]
    }
  }
}
