{
  "schema_version": 2,
  "command": "remove",
  "ok": true,
  "data": {
    "applied": false,
    "items": [
      {
        "bytes_kept": false,
        "dest_dirs": [
          "~/.claude/skills/deploy"
        ],
        "kind": "tracked-local-permanent",
        "name": "deploy",
        "note": "'deploy' was never published — no other copy exists, so removing deletes it permanently (the topos entry is dropped too). Share it first with `topos publish deploy`, or apply with --yes"
      }
    ]
  },
  "warnings": [],
  "next_actions": [
    {
      "code": "APPLY_DESCRIBED",
      "argv": [
        "topos",
        "remove",
        "deploy",
        "--yes"
      ],
      "mutates": true,
      "risk_note": "removes the skill from this machine (a team-managed skill keeps its canonical bytes)"
    }
  ]
}
