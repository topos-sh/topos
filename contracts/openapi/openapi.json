{
  "openapi": "3.1.0",
  "info": {
    "title": "Topos OSS plane",
    "description": "The self-hostable Topos plane — device-signed writes + token-scoped reads, plus the enrollment + governance surface. Every returned protocol outcome of an op_id-carrying write rides in a 200 body (the canonical JsonEnvelope + receipt); non-2xx is reserved for transport/auth/integrity faults.",
    "license": {
      "name": "Apache-2.0"
    },
    "version": "0.0.0"
  },
  "paths": {
    "/i/{token}": {
      "get": {
        "tags": [
          "enrollment"
        ],
        "operationId": "read_invite_bootstrap",
        "parameters": [
          {
            "name": "token",
            "in": "path",
            "description": "The opaque `/i/<token>` invite token.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The invite bootstrap payload (workspace + plane signing root; no bytes, no role).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BootstrapData"
                }
              }
            }
          },
          "404": {
            "description": "No such invite, or it is revoked/expired.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/admin-claim": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "admin_claim",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AdminClaimRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The admin-claim receipt — OK stands up the workspace + seats the first owner; DENIED the flat error.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body or device public key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/current/{read_token}": {
      "get": {
        "tags": [
          "reads"
        ],
        "operationId": "get_current",
        "parameters": [
          {
            "name": "read_token",
            "in": "path",
            "description": "The per-follower read token (resolves to an opaque scope).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "If-None-Match",
            "in": "header",
            "description": "The cached ETag \"<epoch>.<seq>\".",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "Topos-Known-Version-Id",
            "in": "header",
            "description": "The client's known current commit id (hex64) — the commit-sensitive half of the 304.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The signed current record (application/json) + a commit-sensitive ETag.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SignedCurrentRecord"
                }
              }
            }
          },
          "304": {
            "description": "Pointer unchanged (the ETag AND the known version both match)."
          },
          "404": {
            "description": "No such token, or no current pointer yet.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/device/authorize": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "start_device_auth",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeviceAuthorizeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The device-authorization grant (device_code / user_code / verification_uri).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeviceAuthorizeResponse"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body or device public key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "No such invite (or it is revoked/expired).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/device/token": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "poll_device_auth",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeviceTokenRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The poll status (pending/slow_down/denied/expired/granted + the grant on granted).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeviceTokenResponse"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "No such device code.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/enroll/passcode": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "start_passcode",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasscodeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "A constant-shaped ack (the send is fire-and-forget; no enumeration oracle).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasscodeAck"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "No live session for that user code.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/enroll/passcode/confirm": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "complete_passcode",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasscodeConfirmRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The confirmation status (confirmed/wrong_code/expired/too_many_attempts).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasscodeConfirmResponse"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "No live session for that user code.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/enroll/verify/{user_code}": {
      "get": {
        "tags": [
          "enrollment"
        ],
        "operationId": "read_verification_context",
        "parameters": [
          {
            "name": "user_code",
            "in": "path",
            "description": "The user code shown by `device/authorize`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The verification-page disclosure (machine name, fingerprint, workspace, offered skills).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VerificationContextResponse"
                }
              }
            }
          },
          "404": {
            "description": "No live session for that user code.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/invites": {
      "post": {
        "tags": [
          "governance"
        ],
        "operationId": "create_invite",
        "parameters": [
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 governance-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InviteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The invite receipt — OK carries the InviteData (link + seeded roster/skills); DENIED the flat error.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or signature header.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/proposals": {
      "post": {
        "tags": [
          "writes"
        ],
        "operationId": "propose",
        "parameters": [
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 device-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProposeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The proposal receipt (NEEDS_REVIEW on success; CONFLICT / DENIED / … otherwise).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or device signature.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/publish": {
      "post": {
        "tags": [
          "writes"
        ],
        "operationId": "publish",
        "parameters": [
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 device-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PublishRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The publish receipt — EVERY protocol outcome (OK / CONFLICT / APPROVAL_REQUIRED / DENIED / …).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or device signature.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/reverts": {
      "post": {
        "tags": [
          "writes"
        ],
        "operationId": "revert",
        "parameters": [
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 device-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevertRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The revert receipt (OK on success; CONFLICT / DENIED / … otherwise).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or device signature.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/reviews": {
      "post": {
        "tags": [
          "writes"
        ],
        "operationId": "review",
        "parameters": [
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 device-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReviewRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The review receipt (OK on an approve that promoted; CONFLICT / DENIED / … otherwise).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or device signature.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/devices": {
      "post": {
        "tags": [
          "enrollment"
        ],
        "operationId": "redeem",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id (the grant is authoritative for the workspace).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 enrollment-possession signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RedeemRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The redeem receipt — OK carries the registered device + minted read creds; DENIED the flat error.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, key, or signature header.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "governance"
        ],
        "operationId": "revoke_device",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id (REST sugar; the signed workspace_id rides the body).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 governance-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeviceRevokeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The revoke receipt — OK on success (instant revoke), DENIED (a 200) on a role denial.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or signature header.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/policy/review-required": {
      "put": {
        "tags": [
          "policy"
        ],
        "operationId": "set_review_required",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "The workspace id.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Authorization",
            "in": "header",
            "description": "Bearer <admin token> (the plane operator's secret).",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PolicyReviewRequiredRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "The policy was set (an idempotent write)."
          },
          "400": {
            "description": "Malformed body or workspace id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "401": {
            "description": "Admin token configured but missing/invalid on the request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "404": {
            "description": "No admin token is configured on this plane (the route is disabled).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/roster/{email}": {
      "put": {
        "tags": [
          "governance"
        ],
        "operationId": "roster_set",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id (REST sugar; the signed workspace_id rides the body).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "email",
            "in": "path",
            "description": "The target principal whose role is set.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 governance-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RosterSetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The roster-set receipt — OK on success, DENIED (a 200) on a role denial.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or signature header.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "governance"
        ],
        "operationId": "roster_remove",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id (REST sugar; the signed workspace_id rides the body).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "email",
            "in": "path",
            "description": "The target principal to remove from the roster.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Topos-Device-Signature",
            "in": "header",
            "description": "base64url(64-byte Ed25519 governance-op signature), 86 chars",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RosterRemoveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The roster-remove receipt — OK on success, DENIED (a 200) on a role denial.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "400": {
            "description": "Malformed body, identifier, or signature header.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/skills/{skill}/bundles/{object_id}": {
      "get": {
        "tags": [
          "reads"
        ],
        "operationId": "get_bundle",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "skill",
            "in": "path",
            "description": "Skill id (must match the read token's scope).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "object_id",
            "in": "path",
            "description": "The content id (hex64) of the object to fetch.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Authorization",
            "in": "header",
            "description": "Bearer <read_token>.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The raw object bytes (content-addressed, immutable).",
            "content": {
              "application/octet-stream": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "No/blank credential, scope/path mismatch, or unreachable object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/skills/{skill}/proposals": {
      "get": {
        "tags": [
          "reads"
        ],
        "operationId": "list_proposals",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "skill",
            "in": "path",
            "description": "Skill id (must match the read token's scope).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Authorization",
            "in": "header",
            "description": "Bearer <read_token>.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The skill's OPEN, non-stale proposals (a possibly-empty list).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WireProposalList"
                }
              }
            }
          },
          "404": {
            "description": "No/blank credential, or scope/path mismatch (indistinguishable).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    },
    "/v1/workspaces/{ws}/skills/{skill}/versions/{version_id}": {
      "get": {
        "tags": [
          "reads"
        ],
        "operationId": "get_version",
        "parameters": [
          {
            "name": "ws",
            "in": "path",
            "description": "Workspace id.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "skill",
            "in": "path",
            "description": "Skill id (must match the read token's scope).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "version_id",
            "in": "path",
            "description": "The version (commit) id (hex64) whose metadata to read.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Authorization",
            "in": "header",
            "description": "Bearer <read_token>.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The version's authenticated metadata (immutable).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WireVersionMeta"
                }
              }
            }
          },
          "404": {
            "description": "No/blank credential, scope/path mismatch, or unauthorized/unreachable version.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "429": {
            "description": "Rate limited (Retry-After header).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          },
          "500": {
            "description": "Integrity / internal store fault.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonEnvelope"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ActionCode": {
        "type": "string",
        "title": "ActionCode",
        "description": "A machine-actionable next-action code. The values in `examples` are the known set; an unrecognized future code is still valid and MUST be executed via the action's `argv` (never rejected).",
        "examples": [
          "PROPOSE_PUBLISH",
          "REBASE_AND_RETRY",
          "RESOLVE_DIVERGED_DRAFT",
          "APPLY_WAITING_UPDATE",
          "DISAMBIGUATE_NAME",
          "REPIN_PLANE_KEY",
          "REQUEST_ACCESS",
          "RETRY",
          "CONTACT_ADMIN"
        ]
      },
      "AdminClaimRequest": {
        "type": "object",
        "description": "`POST /v1/admin-claim` body — consume a one-time self-host claim token to stand up a workspace + seat its\nfirst owner. The server re-derives the device key id from `device_public_key`.",
        "required": [
          "claim_token",
          "device_public_key",
          "display_name"
        ],
        "properties": {
          "claim_token": {
            "type": "string",
            "description": "The one-time admin-claim token."
          },
          "device_public_key": {
            "type": "string",
            "description": "The claiming device's raw 32-byte Ed25519 public key, base64url-unpadded."
          },
          "display_name": {
            "type": "string",
            "description": "The display name for the standing-up workspace."
          }
        }
      },
      "Affected": {
        "type": "object",
        "description": "What an outcome refers to (every field optional — a policy flip has no skill/version).",
        "properties": {
          "proposal": {
            "type": [
              "string",
              "null"
            ]
          },
          "skill": {
            "type": [
              "string",
              "null"
            ]
          },
          "version": {
            "type": [
              "string",
              "null"
            ],
            "description": "A `version_id` (commit SHA-256, lowercase hex) when the outcome refers to a version."
          },
          "workspace": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "BootstrapData": {
        "type": "object",
        "description": "The payload an `/i/<token>` invite link resolves to — read once, before enrollment, to learn the\nworkspace + the plane signing root to pin. No bytes, no role.",
        "required": [
          "schema_version",
          "invite",
          "plane",
          "workspace"
        ],
        "properties": {
          "invite": {
            "$ref": "#/components/schemas/BootstrapInvite",
            "description": "The invite's non-secret descriptor (consent posture, expiry)."
          },
          "offered_skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BootstrapSkill"
            },
            "description": "The skills the invite pre-offers (a name may be absent → the client shows the id). Empty for a\nmembership-only door."
          },
          "plane": {
            "$ref": "#/components/schemas/BootstrapPlane",
            "description": "The plane the workspace lives on, including the signing key to TOFU-pin."
          },
          "schema_version": {
            "type": "integer",
            "format": "int32",
            "description": "Always `1` for this contract version (the schema pins it `const`).",
            "minimum": 0
          },
          "workspace": {
            "$ref": "#/components/schemas/BootstrapWorkspace",
            "description": "The workspace the invite is for."
          }
        }
      },
      "BootstrapInvite": {
        "type": "object",
        "description": "The invite's non-secret descriptor — the consent posture a redeemer agrees to, never a role.",
        "required": [
          "token_id",
          "consent",
          "first_receive_auto_land"
        ],
        "properties": {
          "consent": {
            "$ref": "#/components/schemas/ConsentMode",
            "description": "The consent posture every first-receive follows."
          },
          "expires_at": {
            "type": [
              "string",
              "null"
            ],
            "description": "The invite's expiry as an RFC-3339 string, if it expires (`None` = never)."
          },
          "first_receive_auto_land": {
            "type": "boolean",
            "description": "Whether a first-received skill auto-lands. **ALWAYS false** — a received skill is offered, never\nsilently applied (a direct human yes is required)."
          },
          "token_id": {
            "type": "string",
            "description": "A stable, non-secret reference for this invite (the link token the client used to reach the payload)."
          }
        }
      },
      "BootstrapPlane": {
        "type": "object",
        "description": "The plane a workspace lives on — its public base URL, deployment posture, offered enrollment method, and\nthe signing key a device pins as its trust root.",
        "required": [
          "base_url",
          "deployment_mode",
          "enrollment_method",
          "signing_key"
        ],
        "properties": {
          "base_url": {
            "type": "string",
            "description": "The plane's public base URL (the verification + invite links are built on it)."
          },
          "deployment_mode": {
            "$ref": "#/components/schemas/DeploymentMode",
            "description": "This plane's deployment posture."
          },
          "enrollment_method": {
            "type": "string",
            "description": "The enrollment method advertised to a bootstrapping device (e.g. `\"device_code\"` / `\"passcode\"`)."
          },
          "signing_key": {
            "$ref": "#/components/schemas/BootstrapSigningKey",
            "description": "The plane's Ed25519 signing key — the trust root a device pins (TOFU) and every later signed\n`current` pointer verifies against."
          }
        }
      },
      "BootstrapSigningKey": {
        "type": "object",
        "description": "The plane's signing key as a device pins it — the algorithm, the key id, and the raw public key.",
        "required": [
          "alg",
          "key_id",
          "value"
        ],
        "properties": {
          "alg": {
            "$ref": "#/components/schemas/SignatureAlg",
            "description": "The signature algorithm — Ed25519 only (the CLOSED kernel enum; an unknown alg fails to deserialize)."
          },
          "key_id": {
            "type": "string",
            "description": "The plane signing key id (the `key_id` carried in every signed `current` pointer)."
          },
          "value": {
            "type": "string",
            "description": "The raw 32-byte Ed25519 public key, base64url-unpadded — the trust root the device pins."
          }
        }
      },
      "BootstrapSkill": {
        "type": "object",
        "description": "One skill an invite pre-offers — its id, plus an optional display name (names ride the invite; a missing\nname means the client shows the id).",
        "required": [
          "skill_id"
        ],
        "properties": {
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "The skill's display name, if the invite carried one (else the client shows the id)."
          },
          "skill_id": {
            "type": "string",
            "description": "The offered skill id."
          }
        }
      },
      "BootstrapWorkspace": {
        "type": "object",
        "description": "The workspace an invite is for — its id, display name, and domain-verification state. No role (the role\nlives server-side on the pre-seeded member rows).",
        "required": [
          "workspace_id",
          "display_name",
          "verified_domain_status"
        ],
        "properties": {
          "display_name": {
            "type": "string",
            "description": "The workspace display name (shown on the verification page + in the agent's disclosure)."
          },
          "verified_domain": {
            "type": [
              "string",
              "null"
            ],
            "description": "The org-domain claim, if any."
          },
          "verified_domain_status": {
            "$ref": "#/components/schemas/VerifiedDomainStatus",
            "description": "The domain-verification state."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace id."
          }
        }
      },
      "ConsentMode": {
        "type": "string",
        "description": "The consent posture of an invite — a CLOSED set. v0 is a single mode: a received skill is disclosed and\nawaits a direct human yes on first receive (never auto-landed).",
        "enum": [
          "direct_human_first_receive"
        ]
      },
      "CurrentRecord": {
        "type": "object",
        "required": [
          "version_id",
          "generation"
        ],
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/Generation"
          },
          "version_id": {
            "type": "string",
            "description": "The full commit SHA-256 (lowercase hex) — the `version_id`. NOT the `bundle_digest`: the\npointer names the version, and the commit transitively pins the bytes."
          }
        }
      },
      "DeploymentMode": {
        "type": "string",
        "description": "A plane's deployment posture — a CLOSED set. `cloud` requires a confirmed identity step at enrollment;\n`self_host` grants membership straight from a valid grant.",
        "enum": [
          "cloud",
          "self_host"
        ]
      },
      "DeviceAuthorizeRequest": {
        "type": "object",
        "description": "`POST /v1/device/authorize` body — begin an RFC-8628 device-authorization flow against an invite. The\nserver SERVER-derives the device key id from `device_public_key` (a client-asserted id is never trusted).",
        "required": [
          "invite_token",
          "device_public_key",
          "machine_name"
        ],
        "properties": {
          "device_public_key": {
            "type": "string",
            "description": "The device's raw 32-byte Ed25519 public key, base64url-unpadded. The server derives the device key id\nfrom it (never a client-asserted id) and binds it into the enrollment possession frame."
          },
          "invite_token": {
            "type": "string",
            "description": "The opaque `/i/<token>` invite token the device enrolls against."
          },
          "machine_name": {
            "type": "string",
            "description": "A human-readable machine name shown on the verification page (a confused-deputy guard, not authority)."
          }
        }
      },
      "DeviceAuthorizeResponse": {
        "type": "object",
        "description": "`POST /v1/device/authorize` response — the RFC-8628 device-authorization grant (the names mirror the RFC).",
        "required": [
          "device_code",
          "user_code",
          "verification_uri",
          "expires_in",
          "interval"
        ],
        "properties": {
          "device_code": {
            "type": "string",
            "description": "The SECRET device code the client polls `device/token` with."
          },
          "expires_in": {
            "type": "integer",
            "format": "int64",
            "description": "The session lifetime, in seconds (RFC-8628 `expires_in`).",
            "minimum": 0
          },
          "interval": {
            "type": "integer",
            "format": "int64",
            "description": "The minimum poll interval, in seconds (RFC-8628 `interval`).",
            "minimum": 0
          },
          "user_code": {
            "type": "string",
            "description": "The short code a human types on the verification page."
          },
          "verification_uri": {
            "type": "string",
            "description": "The verification URL a human visits to approve the session."
          }
        }
      },
      "DeviceRevokeRequest": {
        "type": "object",
        "description": "`DELETE /v1/workspaces/{ws}/devices` body — revoke a registered device key (owner, or the device's own\nprincipal). The revoke is INSTANT (flip `revoked` + drop the device's read tokens in one transaction).",
        "required": [
          "workspace_id",
          "op_id",
          "device_key_id",
          "target_device_key_id"
        ],
        "properties": {
          "device_key_id": {
            "type": "string",
            "description": "The id of the SIGNING device key (the actor; not the target)."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key."
          },
          "target_device_key_id": {
            "type": "string",
            "description": "The id of the device key to revoke."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id (bound into the governance signing frame)."
          }
        }
      },
      "DeviceTokenRequest": {
        "type": "object",
        "description": "`POST /v1/device/token` body — poll a device-authorization session for its grant.",
        "required": [
          "device_code"
        ],
        "properties": {
          "device_code": {
            "type": "string",
            "description": "The SECRET device code from `device/authorize`."
          }
        }
      },
      "DeviceTokenResponse": {
        "type": "object",
        "description": "`POST /v1/device/token` response — the poll `status`, plus the opaque single-use enrollment `grant` ONLY\nwhen `status` is `granted`. A re-poll of a confirmed session re-derives the SAME grant (idempotent issue).",
        "required": [
          "status"
        ],
        "properties": {
          "grant": {
            "type": [
              "string",
              "null"
            ],
            "description": "The opaque single-use enrollment grant — present ONLY when `status` is `granted` (the redeem credential)."
          },
          "status": {
            "$ref": "#/components/schemas/DeviceTokenStatus",
            "description": "The poll status."
          }
        }
      },
      "DeviceTokenStatus": {
        "type": "string",
        "description": "A device-authorization poll status — the RFC-8628 outcomes (snake_case). `granted` carries the opaque\ngrant; every other status carries only itself (no grant).",
        "enum": [
          "pending",
          "slow_down",
          "denied",
          "expired",
          "granted"
        ]
      },
      "Generation": {
        "type": "object",
        "description": "The internal anti-replay counter `(epoch, seq)`. NEVER rendered to a user; the\nagent consumes `expected`/`current` on a CONFLICT.",
        "required": [
          "epoch",
          "seq"
        ],
        "properties": {
          "epoch": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "seq": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "InviteData": {
        "type": "object",
        "description": "`invite` (mint an `/i/` link + optionally seed the roster). A link never carries a role and never\nenrolls on its own. **INFERRED.** Also the `POST /v1/invites` success `data` shape (the OpenAPI body),\nhence the `utoipa::ToSchema` derive alongside `schemars`.",
        "required": [
          "invite_link"
        ],
        "properties": {
          "invite_link": {
            "type": "string",
            "description": "`/i/<token>`."
          },
          "roster_added": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The skills a redeemer joins + follows (empty = a membership-only door)."
          }
        }
      },
      "InviteRequest": {
        "type": "object",
        "description": "`POST /v1/invites` body — mint an `/i/<token>` invite link, seeding the invited emails onto the roster\n(omitted `role` defaults to `member`; the client must sign the same role byte). Returns\n[`InviteData`](crate::results::InviteData) on success.",
        "required": [
          "workspace_id",
          "op_id",
          "device_key_id",
          "emails"
        ],
        "properties": {
          "device_key_id": {
            "type": "string",
            "description": "The id of the signing OWNER's device key (the registry selects the verifying key by this)."
          },
          "emails": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The emails to invite (seeded onto the roster as `invited`), bound as a set in the signing frame."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key (a retry replays the deterministic link + receipt)."
          },
          "role": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/WorkspaceRole",
                "description": "The role the invitees are granted — omitted defaults to `member` (the client signs the same byte)."
              }
            ]
          },
          "skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InviteSkill"
            },
            "description": "The skills the invite pre-offers (each with an optional display name)."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id (bound into the governance signing frame)."
          }
        }
      },
      "InviteSkill": {
        "type": "object",
        "description": "One skill an invite pre-offers, with an optional display name (the name is NOT bound into the invite\nsigning frame — only the skill id is — so a rename never forks the deterministic invite link).",
        "required": [
          "skill_id"
        ],
        "properties": {
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "An optional display name to carry on the invite."
          },
          "skill_id": {
            "type": "string",
            "description": "The offered skill id."
          }
        }
      },
      "JsonEnvelope": {
        "type": "object",
        "description": "The one common envelope every verb emits. Never prompts; prose (TTY) is rendered from the\nSAME typed value where practical.",
        "required": [
          "schema_version",
          "command",
          "ok"
        ],
        "properties": {
          "command": {
            "type": "string",
            "description": "The verb that produced this (`add`, `follow`, `pull`, `list`, `publish`, …)."
          },
          "data": {
            "description": "Command-specific payload (`{}` when empty)."
          },
          "error": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/WireError",
                "description": "The actionable failure detail (when `ok == false`)."
              }
            ]
          },
          "next_actions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NextAction"
            },
            "description": "Stable, machine-actionable next steps — each carries a complete argv."
          },
          "ok": {
            "type": "boolean"
          },
          "receipt": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Receipt",
                "description": "The durable idempotency record — present on **every** terminal op, including failures."
              }
            ]
          },
          "schema_version": {
            "type": "integer",
            "format": "int32",
            "description": "Always `1` for this contract version (the schema pins it `const`).",
            "minimum": 0
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "NextAction": {
        "type": "object",
        "description": "A machine-actionable next step. The `argv` is the ready-to-exec command; `code` lets an agent\nbranch on the known set and still pass through unknowns.",
        "required": [
          "code",
          "argv"
        ],
        "properties": {
          "argv": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "A complete argv array — execute as-is (no TTY parsing)."
          },
          "code": {
            "$ref": "#/components/schemas/ActionCode"
          }
        }
      },
      "PasscodeAck": {
        "type": "object",
        "description": "`POST /v1/enroll/passcode` response — a CONSTANT-shaped ack (always `sent`); the send is fire-and-forget,\nso neither the body nor its latency reveals whether the address was rostered.",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "$ref": "#/components/schemas/PasscodeAckStatus",
            "description": "Always `sent`."
          }
        }
      },
      "PasscodeAckStatus": {
        "type": "string",
        "description": "The constant-shaped status of a started passcode challenge — always `sent`, so a non-rostered address is\nno enumeration oracle (the cloud roster gate is enforced at redeem, never here).",
        "enum": [
          "sent"
        ]
      },
      "PasscodeConfirmRequest": {
        "type": "object",
        "description": "`POST /v1/enroll/passcode/confirm` body — submit a passcode to confirm the session's identity.",
        "required": [
          "user_code",
          "email",
          "code"
        ],
        "properties": {
          "code": {
            "type": "string",
            "description": "The 6-digit passcode the human entered."
          },
          "email": {
            "type": "string",
            "description": "The email the passcode was sent to."
          },
          "user_code": {
            "type": "string",
            "description": "The user code naming the live device-auth session."
          }
        }
      },
      "PasscodeConfirmResponse": {
        "type": "object",
        "description": "`POST /v1/enroll/passcode/confirm` response — the confirmation status.",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "$ref": "#/components/schemas/PasscodeConfirmStatus",
            "description": "The confirmation status."
          }
        }
      },
      "PasscodeConfirmStatus": {
        "type": "string",
        "description": "The outcome of a passcode confirmation (snake_case). A wrong code carries only the status — never the\nattempts remaining (no brute-force timing/count oracle on the wire).",
        "enum": [
          "confirmed",
          "wrong_code",
          "expired",
          "too_many_attempts"
        ]
      },
      "PasscodeRequest": {
        "type": "object",
        "description": "`POST /v1/enroll/passcode` body — start a passcode challenge for an email on a live device-auth session.",
        "required": [
          "user_code",
          "email"
        ],
        "properties": {
          "email": {
            "type": "string",
            "description": "The email the passcode proves control of."
          },
          "user_code": {
            "type": "string",
            "description": "The user code naming the live device-auth session."
          }
        }
      },
      "PointerScope": {
        "type": "object",
        "required": [
          "workspace_id",
          "skill_id"
        ],
        "properties": {
          "skill_id": {
            "type": "string"
          },
          "workspace_id": {
            "type": "string"
          }
        }
      },
      "PolicyReviewRequiredRequest": {
        "type": "object",
        "description": "`PUT /v1/workspaces/{ws}/policy/review-required` body — the self-host operator toggle for the\n`review-required` workspace policy (an idempotent set; JSON so the body stays extensible without a\npath-shape change). Authenticated by the plane's admin token, not a device-op signature; the route is\ninvisible (404) on a plane with no admin token configured.",
        "required": [
          "review_required"
        ],
        "properties": {
          "review_required": {
            "type": "boolean",
            "description": "The desired policy value: `true` gates any direct publish behind a reviewer's approval."
          }
        }
      },
      "ProposeData": {
        "type": "object",
        "description": "`publish --propose` (opens a PR; uploads a full candidate **without moving `current`**). Returns\n`NEEDS_REVIEW`. **INFERRED.**",
        "required": [
          "proposal",
          "base_version_id",
          "title"
        ],
        "properties": {
          "base_version_id": {
            "type": "string"
          },
          "body": {
            "type": [
              "string",
              "null"
            ]
          },
          "proposal": {
            "type": "string",
            "description": "`<skill>@<version_id>` of the candidate."
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ProposeRequest": {
        "type": "object",
        "description": "`POST /v1/proposals` body — opens a proposal (a PR): ingests a full candidate **without moving\n`current` or signing** (`NEEDS_REVIEW`). The authority's `propose` op takes the **same** input shape as\n`publish` (candidate + device + `op_id` + `expected`); there is **no** separate title/body on the op (a\ntitle/body, if ever surfaced, would be composed into the candidate's commit message).",
        "required": [
          "workspace_id",
          "skill_id",
          "op_id",
          "device_key_id",
          "expected",
          "candidate"
        ],
        "properties": {
          "candidate": {
            "$ref": "#/components/schemas/WireCandidate",
            "description": "The full candidate bundle to ingest as the proposal's content."
          },
          "device_key_id": {
            "type": "string",
            "description": "The id of the device key whose signature (in the header) authorizes this op."
          },
          "expected": {
            "$ref": "#/components/schemas/Generation",
            "description": "The `(epoch, seq)` the proposal is born against (its base); a stale base later makes it non-current."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key (replays the stored receipt on retry)."
          },
          "skill_id": {
            "type": "string",
            "description": "The target skill id within the workspace."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id."
          }
        }
      },
      "PublishData": {
        "type": "object",
        "description": "`publish` (a direct publish that moves `current`). On a GENESIS (first) publish the client also folds in\na shareable `/i/` link pre-offering the skill — **best-effort + owner-gated** (minting it signs a\ngovernance op the plane denies for a non-owner), so `invite_link` is `Some` only on a genesis publish by\nan owner, and `None` otherwise. Under `review-required` a direct publish instead returns\n`APPROVAL_REQUIRED` (with the `publish --propose` next-action) and carries no `data`. **INFERRED.**",
        "required": [
          "skill_id",
          "version_id",
          "bundle_digest",
          "current_generation"
        ],
        "properties": {
          "bundle_digest": {
            "type": "string"
          },
          "current_generation": {
            "$ref": "#/components/schemas/Generation",
            "description": "The pointer's new generation after the move."
          },
          "invite_link": {
            "type": [
              "string",
              "null"
            ],
            "description": "A shareable `/i/<token>` invite pre-offering this skill — present ONLY on a genesis publish where the\npublisher could mint it (an owner); `None` on an ordinary publish or a denied/failed mint. The pointer\nmove is the real outcome; the link is a convenience (the `invite` verb mints one independently)."
          },
          "skill_id": {
            "type": "string"
          },
          "version_id": {
            "type": "string",
            "description": "The new commit."
          }
        }
      },
      "PublishRequest": {
        "type": "object",
        "description": "`POST /v1/publish` body — a direct publish that moves `current`. The device signature is the\n`Topos-Device-Signature` header (not a body field); the server stamps `created_at`. Under\n`review-required` the authority refuses this closed with `APPROVAL_REQUIRED`, ingesting nothing.",
        "required": [
          "workspace_id",
          "skill_id",
          "op_id",
          "device_key_id",
          "expected",
          "candidate"
        ],
        "properties": {
          "candidate": {
            "$ref": "#/components/schemas/WireCandidate",
            "description": "The full candidate bundle to ingest + publish."
          },
          "device_key_id": {
            "type": "string",
            "description": "The id of the device key whose signature (in the header) authorizes this op."
          },
          "expected": {
            "$ref": "#/components/schemas/Generation",
            "description": "The `(epoch, seq)` this publish's compare-and-set targets; a stale pair yields `CONFLICT`."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key — the same `op_id` replays the stored receipt byte-for-byte."
          },
          "skill_id": {
            "type": "string",
            "description": "The target skill id within the workspace."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id (the receipt + pointer scope)."
          }
        }
      },
      "Receipt": {
        "type": "object",
        "description": "The ONE canonical receipt across all 11 outcomes — the durable idempotency record (present even\non failures): one stable receipt per op, identical on retry.",
        "required": [
          "schema_version",
          "op_id",
          "command",
          "outcome",
          "workspace_id",
          "created_at"
        ],
        "properties": {
          "bundle_digest": {
            "type": [
              "string",
              "null"
            ],
            "description": "The byte-exact consent hash (lowercase hex)."
          },
          "command": {
            "type": "string"
          },
          "created_at": {
            "type": "string",
            "description": "RFC 3339 timestamp (the plane stamps it; never an ambient clock in `topos-core`)."
          },
          "current_generation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Generation"
              }
            ]
          },
          "details": {
            "description": "Outcome-specific extra detail."
          },
          "expected_generation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Generation"
              }
            ]
          },
          "key_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The signing key id that covered this op."
          },
          "op_id": {
            "type": "string",
            "description": "Client-minted UUIDv4, persisted before the first send."
          },
          "outcome": {
            "$ref": "#/components/schemas/TerminalOutcome"
          },
          "schema_version": {
            "type": "integer",
            "format": "int32",
            "description": "Always `1` for this contract version (the schema pins it `const`).",
            "minimum": 0
          },
          "skill_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "version_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The commit SHA-256 (lowercase hex)."
          },
          "workspace_id": {
            "type": "string"
          }
        }
      },
      "RedeemRequest": {
        "type": "object",
        "description": "`POST /v1/workspaces/{ws}/devices` body — redeem an enrollment grant into a registered device + minted\nper-skill read tokens. The enrollment possession signature rides the `Topos-Device-Signature` header\n(NOT a body field); the server re-derives the device key id from `device_public_key`.",
        "required": [
          "workspace_id",
          "grant",
          "device_public_key"
        ],
        "properties": {
          "device_public_key": {
            "type": "string",
            "description": "The device's raw 32-byte Ed25519 public key, base64url-unpadded (must equal the grant's bound key)."
          },
          "grant": {
            "type": "string",
            "description": "The opaque single-use enrollment grant (from a `granted` device-token poll)."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace the device enrolls into (authoritatively the grant's; echoed for the client's clarity)."
          }
        }
      },
      "RedeemResponse": {
        "type": "object",
        "description": "`POST /v1/workspaces/{ws}/devices` success payload — the confirmed enrollment: the registered device and\nthe minted per-skill read tokens. **NO user token, ever.** Rides in the all-outcome envelope's `data`.",
        "required": [
          "workspace_id",
          "device_key_id"
        ],
        "properties": {
          "device_key_id": {
            "type": "string",
            "description": "The server-derived device key id now registered."
          },
          "read_creds": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RedeemedSkillCred"
            },
            "description": "The minted per-skill read credentials (returned ONCE; only their sha256 is stored server-side)."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace the device enrolled into."
          }
        }
      },
      "RedeemedSkillCred": {
        "type": "object",
        "description": "One minted per-skill read credential — the `0600` at-rest secret a follower stores to pull a skill.",
        "required": [
          "skill_id",
          "read_token"
        ],
        "properties": {
          "expires_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The token expiry in epoch-ms (`None` = non-expiring — per-device revoke is the kill switch)."
          },
          "read_token": {
            "type": "string",
            "description": "The plaintext read token (returned ONCE; only its sha256 is stored server-side)."
          },
          "skill_id": {
            "type": "string",
            "description": "The skill the token reads."
          }
        }
      },
      "RevertData": {
        "type": "object",
        "description": "`revert` (a **forward** git-revert restoring older bytes as a new, higher-`seq` version — never a\npointer rollback, never a delete). `--to` names the GOOD version. **INFERRED.**",
        "required": [
          "skill_id",
          "reverted_to",
          "new_version_id",
          "current_generation"
        ],
        "properties": {
          "current_generation": {
            "$ref": "#/components/schemas/Generation"
          },
          "new_version_id": {
            "type": "string",
            "description": "The new forward-revert commit that carries those bytes."
          },
          "reverted_to": {
            "type": "string",
            "description": "The good version named by `--to` (the bytes being restored)."
          },
          "skill_id": {
            "type": "string"
          }
        }
      },
      "RevertRequest": {
        "type": "object",
        "description": "`POST /v1/reverts` body — a **forward** revert: the server constructs a new 1-parent commit carrying the\n`good` version's bytes on top of `current` (`seq` advances; the pointer never moves backward). There is\n**no candidate** — the server reads `good`'s tree + digest from its provenance and builds the commit.",
        "required": [
          "workspace_id",
          "skill_id",
          "op_id",
          "device_key_id",
          "expected",
          "good",
          "author",
          "message"
        ],
        "properties": {
          "author": {
            "type": "string",
            "description": "The author device id recorded in the forward-revert commit frame."
          },
          "device_key_id": {
            "type": "string",
            "description": "The id of the device key whose signature (in the header) authorizes this op."
          },
          "expected": {
            "$ref": "#/components/schemas/Generation",
            "description": "The `(epoch, seq)` this revert's compare-and-set targets; a stale pair yields `CONFLICT`."
          },
          "good": {
            "type": "string",
            "description": "The GOOD version (the `version_id` whose bytes are restored) as 64-char lowercase hex."
          },
          "message": {
            "type": "string",
            "description": "The forward-revert commit message."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key (replays the stored receipt on retry)."
          },
          "skill_id": {
            "type": "string",
            "description": "The target skill id within the workspace."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id."
          }
        }
      },
      "ReviewData": {
        "type": "object",
        "description": "`review` (`--approve` / `--reject` a proposal). Approve is a compare-and-set on the base; a stale\nbase returns `CONFLICT`. **INFERRED.**",
        "required": [
          "proposal",
          "decision"
        ],
        "properties": {
          "current_generation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Generation",
                "description": "The pointer's new generation when an approval moved `current`."
              }
            ]
          },
          "decision": {
            "$ref": "#/components/schemas/ReviewDecision"
          },
          "proposal": {
            "type": "string",
            "description": "`<skill>@<version_id>` of the reviewed proposal."
          }
        }
      },
      "ReviewDecision": {
        "type": "string",
        "description": "A review verdict. **INFERRED.**",
        "enum": [
          "approve",
          "reject"
        ]
      },
      "ReviewRequest": {
        "type": "object",
        "description": "`POST /v1/reviews` body — a governance decision on an open proposal. `approve` runs the shared\n`(epoch, seq)` compare-and-set on the proposal's base (a stale base ⇒ `CONFLICT`) and, under\n`review_required`, enforces four-eyes (the proposer may not self-approve) before promoting; `reject`\nis a standalone status flip (nothing signed).",
        "required": [
          "workspace_id",
          "skill_id",
          "op_id",
          "device_key_id",
          "expected",
          "proposal",
          "decision"
        ],
        "properties": {
          "decision": {
            "$ref": "#/components/schemas/ReviewDecision",
            "description": "The verdict — `approve` (promote) or `reject`."
          },
          "device_key_id": {
            "type": "string",
            "description": "The id of the device key whose signature (in the header) authorizes this op."
          },
          "expected": {
            "$ref": "#/components/schemas/Generation",
            "description": "The `(epoch, seq)` the approval's compare-and-set targets (the proposal's base); a stale pair on an\n`approve` yields `CONFLICT`."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key (replays the stored receipt on retry)."
          },
          "proposal": {
            "type": "string",
            "description": "The proposal being reviewed, named by its candidate commit id (64-char lowercase hex)."
          },
          "skill_id": {
            "type": "string",
            "description": "The target skill id within the workspace."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id."
          }
        }
      },
      "RosterRemoveRequest": {
        "type": "object",
        "description": "`DELETE /v1/workspaces/{ws}/roster/{email}` body — remove a principal from the workspace roster\n(owner-only). The target principal is the `{email}` path segment; the body carries only the op identity.",
        "required": [
          "workspace_id",
          "op_id",
          "device_key_id"
        ],
        "properties": {
          "device_key_id": {
            "type": "string",
            "description": "The id of the signing owner's device key."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id (bound into the governance signing frame)."
          }
        }
      },
      "RosterSetRequest": {
        "type": "object",
        "description": "`PUT /v1/workspaces/{ws}/roster/{email}` body — set a principal's workspace role (owner-only). The target\nprincipal is the `{email}` path segment; the role rides the body (bound into the signing frame).",
        "required": [
          "workspace_id",
          "op_id",
          "device_key_id",
          "role"
        ],
        "properties": {
          "device_key_id": {
            "type": "string",
            "description": "The id of the signing owner's device key."
          },
          "op_id": {
            "type": "string",
            "description": "The client-minted UUIDv4 idempotency key."
          },
          "role": {
            "$ref": "#/components/schemas/WorkspaceRole",
            "description": "The role to set on the `{email}` target."
          },
          "workspace_id": {
            "type": "string",
            "description": "The target workspace id (bound into the governance signing frame)."
          }
        }
      },
      "Signature": {
        "type": "object",
        "required": [
          "alg",
          "key_id",
          "value"
        ],
        "properties": {
          "alg": {
            "$ref": "#/components/schemas/SignatureAlg"
          },
          "key_id": {
            "type": "string"
          },
          "value": {
            "type": "string",
            "description": "base64url (unpadded) of the raw 64-byte Ed25519 signature — exactly 86 chars."
          }
        }
      },
      "SignatureAlg": {
        "type": "string",
        "description": "The signature algorithm — a CLOSED set. v0 is Ed25519 only; an unknown or `none` algorithm must\nfail to deserialize (fail closed), foreclosing algorithm-confusion / downgrade on the trust-root\npointer. (Contrast [`ActionCode`], deliberately OPEN for forward-compat — an algorithm id is the\nexact opposite of a next-action code and must never silently pass through.)",
        "enum": [
          "Ed25519"
        ]
      },
      "SignedCurrentRecord": {
        "type": "object",
        "description": "The signed `current` pointer — a versioned envelope; `ETag = \"<epoch>.<seq>\"`.",
        "required": [
          "schema_version",
          "scope",
          "record",
          "signature"
        ],
        "properties": {
          "record": {
            "$ref": "#/components/schemas/CurrentRecord"
          },
          "schema_version": {
            "type": "integer",
            "format": "int32",
            "description": "Always `1` for this contract version (the schema pins it `const`).",
            "minimum": 0
          },
          "scope": {
            "$ref": "#/components/schemas/PointerScope"
          },
          "signature": {
            "$ref": "#/components/schemas/Signature"
          }
        }
      },
      "TerminalOutcome": {
        "type": "string",
        "description": "The closed set of terminal outcomes the agent branches on. SCREAMING_SNAKE on the wire.",
        "enum": [
          "OK",
          "APPROVAL_REQUIRED",
          "NEEDS_REVIEW",
          "CONFLICT",
          "DIVERGED",
          "DENIED",
          "UNAVAILABLE",
          "AMBIGUOUS_NAME",
          "KEY_REPIN_REQUIRED",
          "RETRYABLE_FAILURE",
          "PERMANENT_FAILURE"
        ]
      },
      "VerificationContextResponse": {
        "type": "object",
        "description": "`GET /v1/enroll/verify/{user_code}` response — the verification-page disclosure a human reviews before\nconfirming an identity (the RFC-8628 confused-deputy guard). Carries no secret.",
        "required": [
          "machine_name",
          "device_fingerprint",
          "workspace_display_name",
          "verified_domain_status"
        ],
        "properties": {
          "device_fingerprint": {
            "type": "string",
            "description": "A short hex fingerprint of the device's public key — a human cross-checks it against the device. A\ndisplay aid only, never an authority input."
          },
          "machine_name": {
            "type": "string",
            "description": "The human-readable machine name the device offered at start."
          },
          "offered_skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BootstrapSkill"
            },
            "description": "The skills the invite pre-offers (each with an optional display name)."
          },
          "verified_domain": {
            "type": [
              "string",
              "null"
            ],
            "description": "The org-domain claim, if any."
          },
          "verified_domain_status": {
            "$ref": "#/components/schemas/VerifiedDomainStatus",
            "description": "The domain-verification state."
          },
          "workspace_display_name": {
            "type": "string",
            "description": "The workspace display name the device would join."
          }
        }
      },
      "VerifiedDomainStatus": {
        "type": "string",
        "description": "A workspace's domain-verification state — snake_case on the wire.",
        "enum": [
          "unverified",
          "pending",
          "verified"
        ]
      },
      "WireCandidate": {
        "type": "object",
        "description": "A full candidate bundle: every file's bytes, the declared parents, and the author + message — the\nshared input the `publish` and `propose` writes ingest (the `revert` write needs no candidate; the\nserver builds that forward commit from the good version). Maps to `plane-store`'s\n`CandidateUpload { files, parents, author, message }` — each `parents` entry hex-decoded into a\n`CommitId`.",
        "required": [
          "files",
          "parents",
          "author",
          "message"
        ],
        "properties": {
          "author": {
            "type": "string",
            "description": "The author device id recorded in the commit frame."
          },
          "files": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WireFile"
            },
            "description": "Every file in the candidate bundle (each server-rehashed)."
          },
          "message": {
            "type": "string",
            "description": "The commit message (title + body composed into one string)."
          },
          "parents": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The candidate commit's declared parents, each a 64-char lowercase-hex `version_id` (`0` parents for a\ngenesis publish, `1` for a normal publish / propose, `2` for an author merge). Each must already be\npresent in the workspace; a lie changes the recomputed commit id, so the server need not trust it."
          }
        }
      },
      "WireError": {
        "type": "object",
        "description": "The flat wire error (rich `thiserror` enums stay internal). Carries a\nstable code + retryability + the safe next actions; never raw SQL/git strings.",
        "required": [
          "code",
          "outcome",
          "retryable"
        ],
        "properties": {
          "affected": {
            "$ref": "#/components/schemas/Affected"
          },
          "code": {
            "type": "string",
            "description": "A stable, machine-branchable error code, distinct from (and finer than) `outcome`. This is an\n**open, additive** string vocabulary — it is intentionally NOT a closed enum: new codes are\nadded over time, and a consumer must treat an unrecognized `code` as the `outcome` it carries."
          },
          "context": {
            "description": "Structured context (no TTY prose to parse)."
          },
          "current_generation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Generation"
              }
            ]
          },
          "expected_generation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Generation"
              }
            ]
          },
          "next_actions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NextAction"
            }
          },
          "outcome": {
            "$ref": "#/components/schemas/TerminalOutcome"
          },
          "retryable": {
            "type": "boolean"
          }
        }
      },
      "WireFile": {
        "type": "object",
        "description": "One file of a candidate bundle, uploaded **by value**. The raw bytes ride as base64 in the JSON body\n(`content_base64`, standard alphabet); the server base64-decodes them and **rehashes every byte** to\nderive the content id — there is no reference-by-id, and a client hash is never trusted. Maps to\n`plane-store`'s `UploadedFile { path, mode, bytes }` (decode `content_base64` → `bytes`).",
        "required": [
          "path",
          "mode",
          "content_base64"
        ],
        "properties": {
          "content_base64": {
            "type": "string",
            "description": "The raw file bytes, base64-encoded. The server decodes then rehashes them (server-side digest)."
          },
          "mode": {
            "$ref": "#/components/schemas/WireFileMode",
            "description": "The file mode (regular or executable)."
          },
          "path": {
            "type": "string",
            "description": "The bundle-relative, forward-slash path."
          }
        }
      },
      "WireFileMode": {
        "type": "string",
        "description": "A candidate file's mode on the wire — the two git regular-file modes as their literal octal strings\n(`\"100644\"` / `\"100755\"`). A **closed** wire mirror of `topos_core::digest::FileMode`: that kernel enum\nlives in a `no_std` crate `topos-types` does not depend on (and it carries no serde/schema derives), so\nthe wire leaf owns its own copy. The route handler maps it 1:1 at the edge —\n`Regular ⇔ FileMode::Regular`, `Executable ⇔ FileMode::Executable` — for both the inbound candidate\n([`WireFile`]) and the outbound version metadata ([`WireVersionFile`]).",
        "enum": [
          "100644",
          "100755"
        ]
      },
      "WireOpenProposal": {
        "type": "object",
        "description": "One OPEN proposal on the wire — its candidate `version_id` (the `@hash`), the `base_generation` it was\nopened against, and when. The proposals-listing read returns ONLY these three fields: **no bytes, no\nproposer, no roles, no rendered diff**. Mirrors `plane-store`'s `OpenProposalSummary` with the id hex-encoded.",
        "required": [
          "version_id",
          "base_generation",
          "created_at"
        ],
        "properties": {
          "base_generation": {
            "$ref": "#/components/schemas/Generation",
            "description": "The `(epoch, seq)` the proposal was opened against (its base); when `current` advances past it the\nproposal stales and drops out of this list."
          },
          "created_at": {
            "type": "string",
            "description": "When the proposal was opened (the server-stamped RFC-3339 string)."
          },
          "version_id": {
            "type": "string",
            "description": "The proposal's candidate commit id (64-char lowercase hex) — the `<skill>@<version_id>` handle."
          }
        }
      },
      "WireProposalList": {
        "type": "object",
        "description": "`GET /v1/workspaces/{ws}/skills/{skill}/proposals` response body — the OPEN, non-stale proposals on a\nrostered skill (a possibly-empty list, ordered by `(created_at, version_id)`). A staled proposal is absent\n(keep == read == list); the list carries no bytes and no proposer — it is the thin handle a client turns\ninto a `diff` / `review` follow-up.",
        "required": [
          "proposals"
        ],
        "properties": {
          "proposals": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WireOpenProposal"
            },
            "description": "The open proposals (possibly empty)."
          }
        }
      },
      "WireVersionFile": {
        "type": "object",
        "description": "One file of a version's metadata on the wire — its path, mode, and content id (`object_id`), mirroring\n`plane-store`'s `VersionFile` with the id hex-encoded. The **bytes are NOT here**: a client fetches each\nby `object_id` through the bundle (object) read route.",
        "required": [
          "path",
          "mode",
          "object_id"
        ],
        "properties": {
          "mode": {
            "$ref": "#/components/schemas/WireFileMode",
            "description": "The file mode (regular or executable)."
          },
          "object_id": {
            "type": "string",
            "description": "The file's content id (64-char lowercase hex) — the handle the per-blob read route resolves."
          },
          "path": {
            "type": "string",
            "description": "The bundle-relative, forward-slash path."
          }
        }
      },
      "WireVersionMeta": {
        "type": "object",
        "description": "`GET /v1/workspaces/{ws}/skills/{skill}/versions/{version_id}` response body — a version's authenticated\nmetadata: its id, the COMPLETE parent set, display author + message, the consent `bundle_digest`, and the\nper-file `(path, mode, object_id)` leaves. Mirrors `plane-store`'s `VersionMeta` with every 32-byte id\nhex-encoded. Assembled WITHOUT reading any blob bytes; the `bundle_digest` is the pin the client's\nper-blob fetches + its own re-hash must reproduce.",
        "required": [
          "version_id",
          "parents",
          "author",
          "message",
          "bundle_digest",
          "files"
        ],
        "properties": {
          "author": {
            "type": "string",
            "description": "The author device id from the commit frame."
          },
          "bundle_digest": {
            "type": "string",
            "description": "The byte-exact consent hash over the bundle (64-char lowercase hex) — the fetch + re-hash pin."
          },
          "files": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WireVersionFile"
            },
            "description": "The per-file leaves, in the version's recorded order."
          },
          "message": {
            "type": "string",
            "description": "The commit message (title + body as one string)."
          },
          "parents": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The COMPLETE parent set, each a 64-char lowercase-hex commit id (`0` for genesis, `1` normally, `2`\nfor an author merge)."
          },
          "version_id": {
            "type": "string",
            "description": "This version's commit id (64-char lowercase hex)."
          }
        }
      },
      "WorkspaceRole": {
        "type": "string",
        "description": "A workspace-level governance role (the RBAC roster — DISTINCT from the per-skill read roster). snake_case.",
        "enum": [
          "owner",
          "reviewer",
          "member"
        ]
      }
    }
  },
  "tags": [
    {
      "name": "writes",
      "description": "Device-signed writes (publish / propose / revert / review)."
    },
    {
      "name": "reads",
      "description": "Token-scoped reads (current / bundles / versions)."
    },
    {
      "name": "enrollment",
      "description": "Invite bootstrap + the device-auth / passcode / redeem / admin-claim enrollment flow."
    },
    {
      "name": "governance",
      "description": "Owner/admin device-op-signed mutations (invite / roster / revoke)."
    }
  ]
}
